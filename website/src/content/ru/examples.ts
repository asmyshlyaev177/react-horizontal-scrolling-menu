// Russian (ru) — translation of en/examples.ts.
//
// Values only: every key, its order and its type come from the English
// module, and a missing or renamed one is a type error rather than a
// silently English page.
// i18n:meta locale=ru source=en/examples.ts source-blob=ffe3d71c21e3e6e545b59fae6bf1db09ad72e4ee status=translated
import type { ExamplesCopy } from '../types.ts';

/** Copy for the example pages, keyed by the slugs in `examples-manifest.ts`. */
export const examples: ExamplesCopy = {
  'add-item-and-scroll-to-it': {
    meta: {
      title: 'Чипы-фильтры в React: добавить элемент и прокрутить к нему',
      description:
        'Чипы-фильтры в горизонтальном скроллере React: добавьте элемент, затем прокрутите к нему с apiRef и scrollToItem после рендера. Живая демонстрация и полный исходник.',
    },
    title: 'Добавить элемент и прокрутить к нему — паттерн чипов-фильтров',
    lede: 'Бар чипов растёт, когда пользователь выбирает фильтр, и новый чип должен оказаться на экране, а не скрытым за правым краем. Загвоздка: нельзя прокрутить к элементу, который ещё не отрендерен. Этот пример делит работу между обработчиком клика и эффектом.',
    demoHint:
      'Нажмите «Добавить фильтр» — чип появится в конце, и ряд прокрутится, чтобы его показать. Крестик удаляет чип.',
    prose: [
      {
        heading: 'Как это работает',
        body: 'Меню получает `apiRef`, который раскрывает полный API за пределами дерева компонентов. `addItem` делает две вещи: сохраняет новый id в ref `lastAdded`, затем добавляет элемент в состояние. Он намеренно не прокручивает — в этот момент чип лишь состояние, а не DOM.',
      },
      {
        heading: 'Почему прокрутка живёт в эффекте',
        body: '`getItemElementById` ищет элемент в DOM, поэтому прокрутка возможна только после того, как React зафиксировал новый элемент. `useEffect`, зависящий от `items`, запускается ровно в этот момент: читает `lastAdded`, очищает его и вызывает `apiRef.current.scrollToItem(el, ’smooth’, ’end’)`. Очистка ref важна — перерисовки по любой другой причине (выбор, стрелки) тоже попадают в эффект и не должны прокручивать повторно.',
      },
      {
        heading: 'Примечания',
        body: `
          - \`lastAdded\` — это ref, а не состояние: запись в него не должна сама вызывать рендер, а его значение имеет смысл только для самого следующего запуска эффекта.
          - \`’end’\` выравнивает новый чип по правому краю ряда; \`’center’\` работает так же, если нужен по центру.
          - Стрелки здесь используют хуки \`useLeftArrowVisible()\` и \`useRightArrowVisible()\` — краткая форма пары \`useIsVisible(’first’/’last’)\`.
          - Полоса прокрутки скрыта обычным CSS на классе библиотеки \`scroll-container\`; сама прокрутка остаётся нативной.
        `,
      },
    ],
  },

  'bottom-arrows': {
    meta: {
      title: 'Стрелки карусели под меню: кастомное размещение в React',
      description:
        'Разместите стрелки карусели под рядом в React: проп Footer у ScrollMenu рендерит любой макет под меню, включая стрелки. Живая демонстрация и полный исходник.',
    },
    title: 'Разместить стрелки под меню — или где угодно в макете',
    lede: 'Стрелки — не встроенный хром, а компоненты, которые вы передаёте, поэтому размещение — это решение макета, а не настройка библиотеки. Этот пример вообще не передаёт `LeftArrow` или `RightArrow` и рендерит обе кнопки в слоте `Footer` под рядом, рядом с обычным содержимым.',
    demoHint:
      'Стрелки находятся под рядом — они читают тот же VisibilityContext, поэтому по-прежнему отключаются на краях.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`ScrollMenu` принимает компонент `Footer` и рендерит его под контейнером прокрутки, внутри того же `VisibilityContext.Provider`, что и элементы. Футер истории — обычный flex-div с текстом и двумя кнопками-стрелками. Поскольку контекст до него доходит, каждая кнопка вызывает `React.useContext(VisibilityContext)` и получает ровно тот API, что получила бы в боковых слотах — сами стрелки не меняются.',
      },
      {
        heading: 'Состояние стрелок, как всегда',
        body: '`useLeftArrowVisible()` и `useRightArrowVisible()` сообщают, находится ли ряд уже у этого края; история маппит результат в `disabled` и гасит кнопку. Клики вызывают `scrollPrev()` и `scrollNext()`. Ничто из этого не знает и не заботится, где смонтирована кнопка.',
      },
      {
        heading: 'Примечания',
        body: `
          - \`Header\` — зеркальный слот над рядом, с тем же контрактом.
          - Боковые пропы \`LeftArrow\`/\`RightArrow\` — просто предустановленные варианты; одни и те же компоненты стрелок работают в любом месте.
          - Футер не только для стрелок: любой компонент, читающий \`VisibilityContext\`, получает там полный API.
          - Обработчик \`onWheel\` истории делает постраничную прокрутку колесом мыши, а жесты тачпада оставляет нативной прокрутке.
        `,
      },
    ],
  },

  autoplay: {
    meta: {
      title: 'Автовоспроизведение карусели в React с доступной паузой',
      description:
        'Автовоспроизведение для меню прокрутки React: useInterval вызывает scrollNext через apiRef, с паузой при наведении, фокусе, касании и уменьшенной анимации. Живая демонстрация и полный исходник.',
    },
    title: 'Автовоспроизведение с доступным поведением паузы',
    lede: 'Часть с продвижением — одна строка: таймер, вызывающий `scrollNext()` через `apiRef`, поверх того же ядра бесконечного цикла. Инженерия — в том, когда *не* продвигаться: наведение, касание, фокус клавиатуры, кнопка «Пауза», скрытые вкладки, рельсы вне экрана и предпочтения уменьшенной анимации — всё останавливает таймер, каждое по своей причине.',
    demoHint:
      'Наведите, коснитесь или перейдите Tab в рельс — он ставится на паузу; кнопка «Пауза» останавливает его, пока вы не нажмёте «Играть».',
    prose: [
      {
        heading: 'Как это работает',
        body: '`useInterval(cb, active ? interval : null)` — это весь планировщик. `active` сворачивает четыре флага — пауза пользователем, пауза по наведению, пауза по фокусу и `prefers-reduced-motion` — а передача `null` полностью удаляет таймер, так что возобновление запускает свежий полный интервал вместо срабатывания в середине цикла сразу после ухода указателя.',
      },
      {
        heading: 'Тики, которые отказываются работать',
        body: 'Даже активный таймер проверяет перед прокруткой: тик читает `api.menuVisible.current` и `document.visibilityState` и пропускает, если хотя бы один говорит «нет». Скрытая вкладка замораживает IntersectionObserver, поэтому прокрутка там означает продвижение вслепую и дрейф учёта телепорта; рельс, прокрученный за пределы страницы, просто не должен двигаться. Пропущенные тики ничего не стоят — следующий перепроверит.',
      },
      {
        heading: 'Поверхность паузы',
        body: 'Наведение и касание ставят паузу через обработчики обёртки, фокус клавиатуры — через `onFocusCapture`/`onBlurCapture`, а `prefers-reduced-motion` держит автовоспроизведение полностью выключенным. Явная кнопка «Пауза» — это то, что WCAG 2.2.2 на самом деле требует для автоматически продвигающегося контента — одной паузы по наведению недостаточно.',
      },
      {
        heading: 'Примечания',
        body: `
          - Переключатель «Пауза» находится вне обёртки наведения — внутри клик по «Пауза» также вызывал бы паузу по наведению, и кнопку невозможно было бы наблюдать за работой.
          - Цикл берётся из того же хука клонирования-и-телепорта \`useInfiniteLoop\`, что и в примере бесконечного цикла; автовоспроизведение добавляет только таймер и флаги паузы.
          - Анимация прокрутки — это нативная плавная прокрутка браузера — при дефолтном \`noPolyfill\` \`transitionDuration\` не действует.
        `,
      },
    ],
  },

  'mouse-drag': {
    meta: {
      title:
        'Прокрутка перетаскиванием в React: горизонтальное меню без ломания кликов',
      description:
        'Прокрутка перетаскиванием мышью для горизонтального списка React: порог в 5px отделяет перетаскивание от кликов, так что элементы остаются кликабельными. Живая демонстрация и полный исходник.',
    },
    title: 'Прокрутка перетаскиванием мышью — не ломая клики',
    lede: 'Сенсорные пользователи прокручивают горизонтальный список нативно, а пользователям мыши нужна обвязка: нажать, перетащить, отпустить. Сложность не в движении ряда — а в том, что наивная реализация превращает каждое отпускание перетаскивания в случайный клик по элементу. Этот пример разделяет их с помощью небольшого класса `DragDealer` и трёх пропов мыши.',
    demoHint:
      'Захватите любое место ряда и перетащите. Элементы по-прежнему кликабельны — клик после перетаскивания подавляется.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`ScrollMenu` раскрывает каррированные обработчики мыши — `onMouseDown`, `onMouseUp` и `onMouseMove` каждый получает объект API и возвращает обычный обработчик события. Экземпляр `DragDealer` отслеживает одну координату-якорь: на каждом движении он применяет дельту напрямую к `scrollContainer.current.scrollLeft`. Остальное делает нативная прокрутка — ни трансформаций, ни физики, а полоса прокрутки остаётся настоящей.',
      },
      {
        heading: 'Почему клики продолжают работать',
        body: 'Перетаскивание начинается только после того, как указатель сдвинулся более чем на 5px, так что обычный клик никогда не прокручивает. Другое направление — классический баг: `onClick` элемента срабатывает после `mouseup`, поэтому отпускание перетаскивания над карточкой выбрало бы её. `dragStop` немедленно очищает флаг применения, но держит `dragging` установленным ещё на один кадр анимации — обработчики кликов проверяют его и выходят.',
      },
      {
        heading: 'Детали, которые стоит забрать',
        body: `
          - \`dragStart\` отменяет отложенный сброс от предыдущего жеста — без него быстрый второй драг может применить устаревшую дельту.
          - \`onMouseLeave\` на обёртке тоже вызывает \`dragStop\`, так что уход с ряда посреди драга не оставит его застрявшим в состоянии перетаскивания.
          - Касанию ничего из этого не нужно — контейнер является настоящим контейнером прокрутки, так что свайп уже работает.
        `,
      },
    ],
  },

  'save-restore-position': {
    meta: {
      title:
        'Сохранение позиции прокрутки в React: восстановление при перемонтировании или возврате',
      description:
        'Сохраняйте смещение прокрутки в sessionStorage на каждом onUpdate и восстанавливайте его в onInit, чтобы позиция переживала перемонтирования и перезагрузки. Живая демонстрация и полный исходник.',
    },
    title: 'Сохранение и восстановление позиции прокрутки',
    lede: 'Горизонтальный рельс забывает своё смещение при каждом размонтировании: уйдите по маршруту и вернитесь, сверните секцию — и он прыгнет к началу. Этот пример сохраняет смещение при прокрутке и записывает его обратно при монтировании, так что меню появляется ровно там, где пользователь его оставил.',
    demoHint:
      'Прокрутите ряд куда-нибудь, размонтируйте меню, затем смонтируйте снова — рельс вернётся на то же смещение.',
    prose: [
      {
        heading: 'Как это работает',
        body: 'Всю функцию несут два колбэка. `onUpdate` срабатывает, когда состояние видимости меню меняется при прокрутке; `savePos` читает `api.scrollContainer.current.scrollLeft` и пишет в `sessionStorage`. При следующем монтировании `onInit` присваивает сохранённое значение прямо в `scrollLeft` — обычная запись свойства, поэтому восстановление мгновенно, а не анимация, проигрывающаяся перед пользователем.',
      },
      {
        heading: 'Переживая перемонтирования, перезагрузки и навигацию назад',
        body: '`sessionStorage` живёт дольше компонента: клиентские переходы маршрутов, условные рендеры и полные перезагрузки страницы возвращаются к сохранённому смещению, а значение привязано к вкладке, так что две вкладки не перезаписывают друг друга. Для навигации по истории история также задаёт `window.history.scrollRestoration = ’manual’`, не давая собственной системе восстановления прокрутки браузера бороться с ручной на «назад» и «вперёд».',
      },
      {
        heading: 'Примечания',
        body: `
          - Восстановление по сырому \`scrollLeft\` пиксельно-точно и не зависит от того, какие элементы существуют — ни id для запоминания, ни поиска.
          - Кнопка «Перезагрузить» истории меняет \`key\` меню, чтобы форсировать перемонтирование; переключатель размонтирования/монтирования демо — тот же тест, сделанный явным.
          - «Сброс» просто удаляет ключ хранилища — следующее монтирование начнёт с нуля, как первое посещение.
        `,
      },
    ],
  },

  'one-item': {
    meta: {
      title:
        'Слайдер «один элемент на экран» в React: элементы прокрутки на всю ширину',
      description:
        'Элементы на всю ширину в горизонтальном меню прокрутки React: min-width 100% на обёртке элемента даёт слайдер «один элемент на экран». Живая демонстрация и полный исходник.',
    },
    title: 'Один элемент на экран: полноширинный слайдер из того же меню',
    lede: 'Нет режима слайдера, который нужно включать. Меню раскладывает то, что говорит ваш CSS, поэтому одно правило — `min-width: 100%` на обёртке элемента библиотеки — превращает тот же компонент в слайдер: каждая карточка заполняет экран, а обычные стрелки постраничной прокрутки продвигают ровно на один элемент.',
    demoHint:
      'Листайте стрелками — каждый слайд ровно шириной в один экран, и каждый слайд сообщает свою видимость.',
    prose: [
      {
        heading: 'Как это работает',
        body: 'История оборачивает меню в стилизованный контейнер, нацеленный на `.react-horizontal-scrolling-menu--item` — div, который библиотека рендерит вокруг каждого ребёнка, — и задаёт ему `minWidth: ’100%’` плюс центрирование во flex. Теперь каждая обёртка охватывает весь контейнер прокрутки, и одна карточка заполняет экран. Стрелки стандартные: `scrollPrev()` и `scrollNext()` листают по видимой группе, а когда видимая группа — один элемент, страница и элемент — одно и то же.',
      },
      {
        heading: 'Стрелки и колесо',
        body: 'Состояние стрелок берётся из `useLeftArrowVisible()` и `useRightArrowVisible()` — каждая возвращает true, когда ряд стоит у этого края, и история подаёт это в `disabled` и гасит кнопку. Проп `onWheel` получает объект API вместе с событием, поэтому вертикальное колесо мыши листает ряд по знаку `deltaY`. Сначала он вынюхивает тачпады: любая горизонтальная дельта или вертикальная меньше 15 считается жестом тачпада и оставляется нативной прокрутке.',
      },
      {
        heading: 'Примечания',
        body: [
          '- `itemId` у каждого ребёнка — единственное жёсткое требование: по нему элементы отслеживаются и к ним прокручиваются.',
          '- Карточки по-прежнему вызывают `useIsVisible(itemId, true)`; при одном элементе на экран каждый внеэкранный слайд сообщает `visible: false`.',
          '- Полоса прокрутки скрыта обычным CSS на контейнере прокрутки (`scrollbar-width: none` плюс псевдоэлемент WebKit) — этот выбор ваш, а не библиотеки.',
          '- Ширина целиком живёт в вашей таблице стилей. Замените 100% на 50% — получите слайдер по два элемента; библиотека ничего не измеряет.',
        ].join('\n'),
      },
    ],
  },

  performance: {
    meta: {
      title: 'Производительность горизонтального списка React: 5000 элементов',
      description:
        'Горизонтальное меню React, рендерящее 5000 элементов на нативной прокрутке: мемоизированные карточки, один IntersectionObserver, без виртуализации. Живая демонстрация и полный исходник.',
    },
    title: '5000 элементов в одном ряду — виртуализация не нужна',
    lede: 'Обычный совет при нескольких сотнях элементов — браться за виртуализацию. Этот пример рендерит 5000 настоящих DOM-узлов в один `ScrollMenu` и остаётся отзывчивым — нативный overflow-скролл двигает, IntersectionObserver смотрит, а React в основном ничего не делает.',
    demoHint:
      'Перетащите рельс или листайте стрелками — каждая из 5000 карточек настоящий DOM-узел; ничего не виндоузится.',
    prose: [
      {
        heading: 'Где работа не происходит',
        body: 'Прокрутка никогда не входит в React. Рельс — настоящий overflow-контейнер: колесо и касание прокручивают его нативно, а обвязка драга только присваивает `scrollContainer.current.scrollLeft` — ни состояния, ни перерисовок на кадр. Видимость — это единственный экземпляр IntersectionObserver, наблюдающий за всеми 5000 элементами; колбэки приходят пакетами, и только компоненты, подписавшиеся через `useIsVisible`, обновляются, когда их собственный элемент переворачивается. Поэлементной математики прокрутки нигде нет.',
      },
      {
        heading: 'Что настраивает история',
        body: '`Card` обёрнут в `React.memo` с компаратором по `selected` и `title`, поэтому выбор одной карточки не сверяет остальные 4999. Индикатор видимости проходит через `useDeferredValue`: после прыжка страницы сотни элементов разом переворачивают состояние, и отсрочка убирает этот всплеск с критического пути вызвавшего его взаимодействия. `noPolyfill={true}` заставляет программную прокрутку использовать собственный `scrollIntoView` браузера вместо полифила плавной прокрутки. Драг — тот же паттерн `DragDealer`, что и в примере mouse-drag.',
      },
      {
        heading: 'Компромисс, который признаёт эта страница',
        body: 'Демо-рельс выше не рендерится на сервере: 5000 карточек сериализуются примерно в мегабайт HTML, поэтому рельс монтируется только на клиенте за подогнанным по высоте плейсхолдером, и сдвига раскладки нет. Это настоящая цена такого размера — браузер спокойно справляется с 5000 живыми узлами, но отправка их как SSR-полезной нагрузки — отдельное решение. Где-то в десятках тысяч узлов настигают и память, и стоимость начального рендера; вот там виндоузинг перестаёт быть необязательным.',
      },
      {
        heading: 'Примечания',
        body: [
          '- DOM для 5000 карточек строится один раз, при монтировании — `React.memo` превращает последующие рендеры родителя в no-op для каждой карточки.',
          '- Стрелки листают примерно по вьюпорту за раз, поэтому проход всего рельса стрелками по задумке медленный — флики драга или прыжки `scrollToItem` лучше подходят этому масштабу.',
          "- Стрелки по-прежнему работают на `useIsVisible('first')` и `useIsVisible('last')` — тот же механизм наблюдателя, что и в меню на десять элементов, при 500-кратном числе элементов.",
        ].join('\n'),
      },
    ],
  },

  progress: {
    meta: {
      title:
        'Индикатор прогресса горизонтальной прокрутки в React для карусели',
      description:
        'Полоса прогресса для горизонтального меню React: подпишитесь на onUpdate, посчитайте видимые элементы, выведите текущую страницу. Живая демонстрация и полный исходник истории.',
    },
    title: 'Добавить индикатор прогресса прокрутки в горизонтальное меню',
    lede: 'Карусель, которая прячет полосу прокрутки, всё равно должна ответить пользователю на вопрос «сколько осталось?». Меню уже знает: оно отслеживает видимость каждого элемента, поэтому позиция — вопрос подсчёта. История рендерит нумерованные кнопки страниц плюс счётчики оставшихся слева/справа; это демо сводит ту же математику к полосе прогресса.',
    demoHint:
      'Прокручивайте ряд, перетаскивайте или используйте стрелки — полоса заполняется постранично, а счётчик показывает, где вы.',
    prose: [
      {
        heading: 'Как это работает',
        body: 'Индикатор передаётся как проп `Footer`, поэтому `ScrollMenu` рендерит его внутри меню, где доступен `VisibilityContext`. Из контекста он берёт `items` — карту за отслеживанием видимости — и подписывается через `items.subscribe(’onUpdate’, cb)`. Это событие срабатывает на каждом колбэке IntersectionObserver, поэтому история дебаунсит его (таймаут плюс `requestAnimationFrame`) перед чтением `items.getVisible()`.',
      },
      {
        heading: 'От видимых элементов к номеру страницы',
        body: 'Число видимых элементов — это размер страницы. Всего страниц — `Math.ceil(items.size / visibleItemsLen)`; текущая страница берётся из `index` последней видимой записи. История превращает их в кликабельные кнопки страниц — каждая вызывает `scrollToItem(getItemByIndex(itemInd))`, обращаясь к элементу по позиции без знания id, — и выводит счётчики оставшихся слева и справа из тех же чисел. Полоса демо — это просто `currentPage / totalPages` как процент ширины.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Ничего не меряется в пикселях — математика целиком на данных видимости, поэтому работает и при разной ширине элементов.',
          '- Измените размер вьюпорта — и размер страницы последует: помещается больше элементов, `getVisible()` возвращает больше записей, а число страниц пересчитывается на следующем обновлении.',
          '- Эффект возвращает очистку, которая вызывает `items.unsubscribe` и гасит отложенный таймер — пропустите её, и размонтированный футер продолжит вызываться.',
          '- До первого отчёта наблюдателя `getVisible()` пуст; история возвращает `null` до этого момента, а демо рисует пустой трек.',
        ].join('\n'),
      },
    ],
  },

  'scroll-to-item': {
    meta: {
      title: 'Прокрутка к элементу в горизонтальном списке React: scrollToItem',
      description:
        'Прокрутите горизонтальный список React к любому элементу по id: onInit передаёт api, а scrollToItem приводит цель в видимость. Живая демонстрация и полный исходник.',
    },
    title: 'Прокрутка к конкретному элементу в горизонтальном списке',
    lede: 'Глубокая ссылка в ряд: чат открывается на активном диалоге, галерея — на фотографии, которой вы поделились. Контейнер прокрутки живёт внутри библиотеки, но ref в его DOM вам не нужен — `onInit` передаёт api, а `scrollToItem` делает позиционирование.',
    demoHint:
      'Рельс не монтируется на Tokyo — onInit сразу прыгает на quito. Перетащите в другое место и перемонтируйте, чтобы увидеть, как он снова приземляется туда.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`ScrollMenu` принимает колбэк `onInit` и вызывает его, когда меню отрендерилось и измерило свои элементы, передавая тот же объект api, что `VisibilityContext` предоставляет внутри. Обработчик ищет элемент через `getItemElementById(id)` и передаёт его в `scrollToItem(item, ’auto’, ’start’)`. Поскольку `onInit` срабатывает только после измерения, поиск не может вернуться пустым для отрендеренного элемента — ни `setTimeout`, ни цикла повторов.',
      },
      {
        heading: 'Поведение и выравнивание',
        body: 'История передаёт `’auto’` и `’start’`: `’auto’` прыгает без анимации — именно это нужно для начальной позиции, ведь пользователь никогда не видит рельс на первом элементе. `’start’` выравнивает левый край элемента по рельсу. Для прокрутки по клику тот же вызов принимает `’smooth’` и `’center’` — это пример «по центру по клику» ниже.',
      },
      {
        heading: 'Примечания',
        body: [
          '- `getItemElementByIndex` — позиционная альтернатива, когда вы знаете слот, но не id.',
          '- Передаваемый id — это `itemId` элемента, тот же ключ, что меню использует для отслеживания видимости.',
          '- Демо воспроизводит поведение, перемонтируя меню с новым `key`; каждое свежее монтирование снова запускает `onInit`.',
        ].join('\n'),
      },
    ],
  },
  'center-on-click': {
    meta: {
      title:
        'Прокручиваемые вкладки React: центрировать активную вкладку по клику',
      description:
        'Прокручиваемые вкладки в React без Material UI: клик по вкладке центрирует её через scrollToItem(el, "smooth", "center"). Живая демонстрация и полный исходник истории.',
    },
    title: 'Центрировать кликнутый элемент — паттерн прокручиваемых вкладок',
    lede: 'Поведение, которое нужно каждой полосе вкладок и которого ни один контейнер прокрутки не даёт бесплатно: кликните вкладку у края — и она скользит в середину, показывая соседей с обеих сторон. Здесь это один вызов API — без Material UI, без измерений, без математики прокрутки.',
    demoHint:
      'Кликните вкладку у любого края — она активируется и центрирует себя в ряду.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`handleItemClick` каррирован: он принимает `itemId` и возвращает функцию, ожидающую объект API. Клик сначала сохраняет id в состояние `selected`, затем вызывает `api.getItemElementById(itemId)`, чтобы найти настоящий DOM-элемент, и передаёт его в `api.scrollToItem(item, ’smooth’, ’center’)`. Один клик, два эффекта: вкладка выбрана и отцентрирована.',
      },
      {
        heading: 'Откуда берётся API',
        body: 'Родительский компонент никогда не держит ref на API. Каждая `Card` читает полный API из `VisibilityContext` — доступного любому ребёнку `ScrollMenu` — и передаёт его в обработчик клика: `onClick(visibility)`. Если же нужно прокручивать извне меню, это паттерн `apiRef` из примера scroll-to-item.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Третий аргумент `scrollToItem` принимает те же значения, что и опция `inline` у `scrollIntoView` — `’start’`, `’center’` или `’end’`.',
          '- Карточки фокусируемы (`role="button"`, `tabIndex=0`) и обрабатывают Enter в `onKeyDown`, так что пользователи клавиатуры получают то же «выбор и центр».',
          '- Обработчик `onWheel` маппит дельты колеса мыши в `scrollNext`/`scrollPrev`, но отступает для тачпадов — горизонтальная дельта или крошечная вертикальная считается жестом и оставляется нативной.',
          '- Стрелки отключают себя сокращениями `useIsVisible(’first’)` и `useIsVisible(’last’)`.',
        ].join('\n'),
      },
    ],
  },

  'swipe-desktop': {
    meta: {
      title: 'Свайп мышью на десктопе: жест флика карусели в React',
      description:
        'Десктопный свайп для горизонтального меню React: отслеживайте нажатие/отпускание мыши, а отпускание дальше 50px фликает на следующую страницу плавным скольжением. Демо и полный исходник.',
    },
    title: 'Свайп на десктопе: флик мышью, который листает меню',
    lede: 'Прокрутка перетаскиванием двигает ряд 1:1 с курсором. Это другой жест мыши — флик. Нажмите, сдвиньте хотя бы на 50px, отпустите — и меню плавно проскользит на страницу в ту сторону через `scrollNext` или `scrollPrev`. Ряд вообще не следует за указателем; скольжение — это плавная программная прокрутка библиотеки, и именно она придаёт отпусканию ощущение инерции.',
    demoHint:
      'Нажмите в любом месте ряда, сдвиньтесь влево или вправо хотя бы на 50px и отпустите — меню проскользит на страницу. Более короткие движения ничего не делают.',
    prose: [
      {
        heading: 'Как это работает',
        body: 'Хук `useSwipe` возвращает три каррированных пропа мыши, которые ожидает `ScrollMenu`, — каждый получает объект API и возвращает обычный обработчик события. `onMouseDown` закрепляет `clientX` указателя в ref, `onMouseMove` постоянно перезаписывает конечную координату, а `onMouseUp` сравнивает их: горизонтальная разница больше `minSwipeDistance` (50px) вызывает `apiObj.scrollNext()` при флике влево или `apiObj.scrollPrev()` при флике вправо.',
      },
      {
        heading: 'Почему кликам не нужна особая обработка',
        body: 'В примере перетаскивания отпускание драга над карточкой кликнуло бы её, поэтому флаг `dragging` должен переживать жест на один кадр. Флик обходит всю проблему: ниже порога в 50px `onMouseUp` ничего не делает, так что клик — это просто клик, — а выше него указатель всё равно уже ушёл с карточки, на которой был нажат. Ни флагов, ни подавленных обработчиков.',
      },
      {
        heading: 'Что история добавляет для касания и колеса',
        body: 'История также фиксирует нативный тач-панораминг: React 18+ регистрирует слушатели `touchmove` как пассивные, поэтому `preventDefault` работает только из непассивного слушателя. Эффект добирается до контейнера прокрутки через `apiRef` (`ref.current.scrollContainer.current`) и навешивает слушатель с `{ passive: false }`. Его обработчик `onWheel` тоже листает меню, с эвристикой — ненулевой `deltaX` или малый `deltaY` считается тачпадом и оставляется в покое.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Координаты живут в ref, а не в состоянии — отслеживание `mousemove` в состоянии перерисовывало бы на каждом пикселе.',
          '- Демо заново закрепляет конечную координату на `mousedown`, так что остаточная позиция от предыдущего жеста никогда не засчитается в новый свайп.',
          '- Настраивайте `minSwipeDistance` по вкусу: ниже — резче, выше — терпимее к небрежным кликам. Сенсорный вариант рецепта использует 20px.',
        ].join('\n'),
      },
    ],
  },

  'mobile-swipe-only': {
    meta: {
      title: 'Скрыть стрелки карусели на мобильных: сенсорная прокрутка React',
      description:
        'Стрелки на десктопе, только сенсорная прокрутка на мобильных для горизонтального меню React: проверка matchMedia на pointer: coarse их скрывает. Живая демонстрация и полный исходник.',
    },
    title:
      'Скрыть стрелки на мобильных — на малых экранах только сенсорная прокрутка',
    lede: 'На сенсорном экране кнопки-стрелки — мёртвый груз: свайп нативен, большие пальцы закрывают цели касания, а каждая стрелка съедает ширину ряда. Демо оставляет стрелки для пользователей мыши и размонтирует их, когда указатель — палец; история идёт дальше и заменяет нативный панораминг явными жестами «свайп-страница».',
    demoHint:
      'Откройте на телефоне или включите эмуляцию касания в DevTools — стрелки исчезнут, и всю работу сделает свайп.',
    prose: [
      {
        heading: 'Как демо скрывает стрелки',
        body: '`LeftArrow` и `RightArrow` — необязательные пропы: передайте `undefined`, и слот вообще не рендерится, так что скрывать через CSS нечего и в порядке табуляции не остаётся кнопок. Переключатель — проверка `matchMedia(’(pointer: coarse)’)` в эффекте: сервер не может знать тип указателя, поэтому первая отрисовка десктопная, со стрелками, а гидрация убирает их, как только грубый указатель подтверждён. Слушатель `change` держит это живым — эмуляция устройства в DevTools переворачивает без перезагрузки.',
      },
      {
        heading: 'Что история делает при касании',
        body: 'Хук `useSwipe` истории превращает свободное панораминг в листание. Каррированные пропы `onTouchStart`, `onTouchMove` и `onTouchEnd` каждый получает объект API; start сбрасывает конечную координату и записывает `targetTouches[0].clientX`, move отслеживает её, а end измеряет пройденное расстояние. Больше `minSwipeDistance` (20px) — вызывает `apiObj.scrollPrev()` или `apiObj.scrollNext()` — одно плавное перелистывание на свайп, независимо от скорости пальца.',
      },
      {
        heading: 'Подавление нативной сенсорной прокрутки',
        body: 'Чтобы листание было единственным движением, собственный панораминг браузера должен остановиться, а React 18+ регистрирует слушатели `touchmove` как пассивные, где `preventDefault` игнорируется. Эффект истории добирается до реального элемента прокрутки через `apiRef` (`ref.current.scrollContainer.current`) и навешивает собственный слушатель с `{ passive: false }`, где вызов работает.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Выбирайте SSR-дефолт осознанно: рендер стрелок первым благоволит краулерам и десктопным пользователям, а сенсорные устройства теряют их сразу после гидрации.',
          '- `(pointer: coarse)` нацелен на ввод, а не на размер экрана — узкое десктопное окно сохраняет стрелки, а планшет нет.',
          '- Если нужно только скрыть стрелки и оставить нативный свайп (поведение демо), пропустите эффект `touchmove` истории — свободный панораминг и скрытые стрелки мирно сосуществуют.',
          '- Сенсорный порог — 20px против 50px десктопного флика — смотрите пример swipe-on-desktop для варианта с мышью.',
        ].join('\n'),
      },
    ],
  },

  'infinite-loop': {
    meta: {
      title: 'Меню прокрутки с бесконечным циклом в React: бесшовная карусель',
      description:
        'Бесшовная циклическая карусель в React без карусельной библиотеки: клоны на обоих концах и один телепорт scrollLeft при успокоении прокрутки. Демо и полный исходник.',
    },
    title: 'Бесконечно зацикленное меню, построенное на публичном API',
    lede: 'Классический трюк карусели «клонируй и телепортируй», реализованный с нулём изменений библиотеки: ряд клонируется на оба конца, и когда прокрутка успокаивается внутри зоны клонов, `scrollLeft` прыгает ровно на длину одного цикла. Кадры по обе стороны прыжка идентичны, так что ничего не выглядит движущимся. Стрелки, колесо, касание и перетаскивание мышью — все пересекают шов.',
    demoHint:
      'Продолжайте двигаться в любую сторону — стрелками, колесом, касанием или драгом — ряд никогда не заканчивается.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`getSlides` копирует элементы на оба конца ряда. Поскольку `itemId` должен быть уникален, клоны получают суффикс — `-lc` слева, `-rc` справа, — сохраняя настоящий id как `realId` для заголовков, выбора и кликов. `useInfiniteLoop` собирает остальное: `normalize()` измеряет длину цикла по `offsetLeft` первого настоящего элемента и его правого клона и сдвигает `scrollLeft` ровно на это расстояние, когда позиция оказывается внутри зоны клонов. Чистая геометрия, и идемпотентно — вызов, когда чинить нечего, ничего не делает.',
      },
      {
        heading: 'Когда срабатывает телепорт',
        body: 'Прыжок посреди прокрутки видимо боролся бы с браузером, поэтому `normalize` работает, когда прокрутка успокаивается: нативный слушатель `scrollend` на контейнере (доступном через проп `containerRef`), с запасным `onScroll` с дебаунсом 150 мс для Safari, который не вызывает `scrollend`. Ещё один прыжок происходит до того, как кто-либо что-то увидит: эффект раскладки задаёт начальный `scrollLeft` на первый настоящий элемент до отрисовки, так что страница никогда не открывается на левых клонах.',
      },
      {
        heading: 'Пересечение шва посреди драга',
        body: 'Колбэк перетаскивания мышью добавляет каждую дельту к `scrollLeft` и вызывает `loop.normalize()` прямо там, внутри жеста. Без этого перетаскивание в зону клонов ждало бы конца драга до телепорта — с ним можно бесконечно тащить через шов и не заметить этого.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Стрелки здесь кастомные и всегда включены: стандартные хуки `first`/`last` отслеживают крайние элементы, а здесь это клоны — они мигнули бы отключёнными на шве.',
          '- Карточки показывают видимость двойного объединения — элемент считается видимым, когда виден он или любой из клонов, — потому что поэлементный флаг устаревает на кадр после телепорта и мигал бы заголовком.',
          '- По две страницы клонов с каждой стороны: зона должна покрывать полный вьюпорт (идентичные кадры вокруг прыжка) с запасом, чтобы клик Next со страницы, straddling шва, никогда не зажимался на конце ряда.',
          '- Всё используемое здесь — `containerRef`, `onScroll`, `itemId`, каррированные пропы мыши — это публичный API.',
        ].join('\n'),
      },
    ],
  },

  simple: {
    meta: {
      title: 'Горизонтальное меню прокрутки React: пример «Начало работы»',
      description:
        'Минимальная настройка react-horizontal-scrolling-menu: элементы с itemId, две стрелки, читающие VisibilityContext, и отслеживание видимости по элементам. Полный исходник.',
    },
    title: 'Начало работы: горизонтальное меню прокрутки в React',
    lede: 'Самая маленькая полезная настройка: ряд карточек, две кнопки-стрелки и то, о чём на самом деле эта библиотека, — каждая карточка знает, на экране ли она. Один компонент, один обязательный проп, один импорт таблицы стилей.',
    demoHint:
      'Прокрутите ряд — стрелки отключаются на краях, и каждая карточка отслеживает свою видимость.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`ScrollMenu` рендерит ваши дочерние элементы внутри нативного контейнера прокрутки и наблюдает за каждым через IntersectionObserver. Единственный контракт — `itemId`, уникальный проп у каждого ребёнка, по которому элементы отслеживаются, находятся и к ним прокручиваются. Внутри любого ребёнка или стрелки `VisibilityContext` передаёт вам полный API.',
      },
      {
        heading: 'Хук видимости',
        body: 'Карточки вызывают `useIsVisible(itemId)`, чтобы подписаться на своё состояние на экране — ни слушателей прокрутки, ни математики позиций, и перерисовываются только затронутые карточки при изменении видимости. Стрелки используют сокращения `first` и `last`, чтобы отключать себя на концах ряда.',
      },
      {
        heading: 'Примечания',
        body: [
          '- `styles.css` — отдельный импорт — JS-бандл никогда не внедряет CSS.',
          '- Ширина элемента — ваш CSS; меню ничего не измеряет и поставляет 210 байт стилей раскладки.',
          '- Второй аргумент `useIsVisible(itemId, true)` — значение, используемое до отчёта наблюдателя, — и значение, которое рендерит ваш сервер, если вы рендерите меню на сервере.',
        ].join('\n'),
      },
    ],
  },

  vertical: {
    meta: {
      title: 'Вертикальное меню прокрутки в React со стрелками',
      description:
        'Сделайте react-horizontal-scrolling-menu вертикальным: flex-column контейнер прокрутки, ограниченная высота, стрелки сверху и снизу через Header/Footer. Живая демонстрация и исходник.',
    },
    title: 'Вертикальное меню прокрутки — тот же компонент, повёрнутый CSS',
    lede: 'Пропа `vertical` нет, и она не нужна: меню — это flex-ряд внутри нативного контейнера прокрутки, так что направить его вниз — пара CSS-переопределений. Отслеживание видимости, хуки стрелок и `scrollPrev`/`scrollNext` продолжают работать на новой оси.',
    demoHint:
      'Крутите колесо над колонкой или используйте стрелки — Up и Down это Header и Footer у ScrollMenu. Ряды гаснут, покидая экран.',
    prose: [
      {
        heading: 'Два переопределения и одна граница высоты',
        body: 'История перестилизует два имени классов библиотеки. Контейнер прокрутки получает `flex-direction: column`, `overflow-y: auto` и `height: initial` вместо дефолтного `max-content`; обёртка получает `height: 100%`, так что любая фиксированная высота родителя становится границей прокрутки. Это и есть весь вертикальный режим. История применяет переопределения через emotion; демо на этой странице вместо этого передаёт утилиты Tailwind через пропы `wrapperClassName` и `scrollContainerClassName` — подходит любой способ стилизации, имена классов стабильны.',
      },
      {
        heading: 'Стрелки становятся Header и Footer',
        body: "Слоты `LeftArrow`/`RightArrow` рендерятся сбоку от рельса — неправильное место для колонки. `ScrollMenu` также принимает компоненты `Header` и `Footer`, рендерящиеся сверху и снизу, и история монтирует там свои кнопки Up и Down. Это обычные потребители `VisibilityContext`: `useIsVisible('first', true)` отключает Up наверху, `useIsVisible('last', false)` отключает Down внизу. Клики передают третий аргумент — `scrollPrev(undefined, undefined, 'end')` и `scrollNext(undefined, undefined, 'start')` — позицию `block` для `scrollIntoView`. `'end'` кладёт предыдущий элемент у нижнего края (на целую страницу вверх); `'start'` ставит следующий элемент наверх (на целую страницу вниз). При дефолтном `'nearest'` каждый клик лишь слегка вталкивал бы следующую строку в видимость.",
      },
      {
        heading: 'Удержание прокрутки внутри колонки',
        body: "`scrollIntoView` двигает каждого прокручиваемого предка своей цели, и страница — один из них — так что прыжок с выравниванием `block` внутри колонки тащит за собой весь документ. Опция, останавливающая это шествие, — `boundary`, передаваемая четвёртым аргументом: `scrollNext(undefined, undefined, 'start', { boundary })` с собственным `scrollContainer.current` меню прокручивает только ряды и ничего больше. Ей нужен `noPolyfill={false}` на `ScrollMenu`, поскольку `boundary` понимает только полифил, — демо выше передаёт и то и другое. Горизонтальные меню редко с этим сталкиваются: их дефолтный `block: 'nearest'` с самого начала не просит у страницы вертикального движения.",
      },
      {
        heading: 'У видимости нет оси',
        body: '`useIsVisible` опирается на IntersectionObserver, а пересечение измеряется в обоих измерениях — ряды сообщают своё состояние при пересечении верхнего и нижнего краёв ровно так же, как горизонтальные элементы делают это сбоку. Демо гасит ряды вне экрана, чтобы это показать, и делает первые четыре серверно-отрисованными видимыми через аргумент `defaultValue` хука.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Единственная фиксированная размерность — это инлайновая высота панели; `height: 100%` обёртки переносит её вниз к контейнеру прокрутки.',
          '- Колесо и касание прокручивают колонку нативно — `overflow-y: auto` делает её настоящим контейнером прокрутки; стрелки — удобство, а не механизм.',
          '- Второй аргумент `scrollPrev`/`scrollNext` — позиция `inline` (горизонтальная) — вертикальным меню важен `block`, поэтому история передаёт его явно.',
        ].join('\n'),
      },
    ],
  },

  rtl: {
    meta: {
      title: 'Горизонтальная прокрутка RTL в React: меню справа налево',
      description:
        'Горизонтальное меню прокрутки справа налево в React: проп RTL разворачивает направление прокрутки и пагинацию, а стрелки меняются местами. Живая демонстрация и полный исходник.',
    },
    title: 'Горизонтальное меню справа налево',
    lede: 'Для интерфейсов на арабском или иврите ряд должен начинаться у правого края и расти влево. Один булев проп разворачивает контейнер прокрутки; единственная оставшаяся работа — решить, что означают стрелки, когда «вперёд» указывает влево.',
    demoHint:
      'Переключите тумблер — ряд перезапустится с противоположного края, и стрелки поменяются ролями.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`RTL={true}` переводит контейнер прокрутки в режим справа налево: первый элемент сидит у правого края, а прокрутка продвигается влево. Всё логическое остаётся логическим — `useIsVisible(’first’)` по-прежнему означает первый элемент в ваших данных, `scrollNext()` по-прежнему движется к последнему — разворачивается только направление на экране.',
      },
      {
        heading: 'Стрелки меняются слотами, а не логикой',
        body: 'Проп `LeftArrow` всегда рендерится на левой стороне экрана. В RTL эта сторона — дом «вперёд», поэтому история подаёт в слоты обменянные элементы: `LeftArrow={RTL ? <RightArrow /> : <LeftArrow />}`. Сами компоненты сохраняют логику — тот, что привязан к `scrollPrev`, всё ещё отключается через `useIsVisible(’first’)` — меняются только их позиция на экране и подпись.',
      },
      {
        heading: 'Примечания',
        body: [
          '- История передаёт `noPolyfill={true}`, поэтому программная прокрутка использует нативную плавную прокрутку браузера вместо встроенного полифила.',
          '- `scrollPrev(’smooth’, ’end’)` и `scrollNext(’smooth’, ’start’)` передают явное выравнивание — второй аргумент это тот же набор `start/center/end`, что принимает `scrollToItem`.',
          '- История переключает `RTL` вживую с чекбокса — проп это просто состояние, ничего в меню не настраивается на этапе сборки.',
        ].join('\n'),
      },
    ],
  },

  'add-items': {
    meta: {
      title: 'Бесконечная горизонтальная прокрутка React: подгрузка в конце',
      description:
        'Бесконечная горизонтальная прокрутка в React: onUpdate проверяет api.items.last().visible и добавляет следующую партию с элементом-загрузчиком. Живая демонстрация и полный исходник.',
    },
    title: 'Подгружать элементы, когда конец входит в видимость',
    lede: 'Бесконечная горизонтальная прокрутка без слушателя прокрутки: меню уже знает, какие элементы видимы, так что «добрался ли пользователь до конца» — это просто вопрос: последний элемент на экране? `onUpdate` задаёт его после каждой прокрутки и добавляет следующую партию, когда ответ «да».',
    demoHint:
      'Прокрутите к правому краю — появится карточка-загрузчик, и прибудет следующая партия. Демо останавливается на 30 элементах.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`onUpdate` срабатывает, когда меняется видимость элементов. Обработчик читает `api.items.last()?.visible` — библиотека отслеживает каждый элемент по его `itemId` и хранит флаг видимости на элемент, поэтому обнаружение конца стоит одной подстановки, без собственного IntersectionObserver и без математики позиции прокрутки. Затем `pushNewItems` имитирует запрос: таймаут в одну секунду, ещё пять элементов, готово.',
      },
      {
        heading: 'Защита запроса',
        body: 'Обновления видимости приходят пачками, поэтому обработчик должен быть безопасен для повторных вызовов. Флаг `loading` делает его идемпотентным: и `onUpdate`, и `pushNewItems` проверяют его, и только первый триггер запускает запрос. Тот же флаг рендерит компонент `Loader` как настоящий элемент меню (со своим `itemId`), который вызывает `scrollIntoView()` при монтировании, удерживая конец ряда в видимости, пока грузится партия.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Правая стрелка передаётся как элемент, `RightArrow={<RightArrow disabled={...} />}` — работают и форма компонента, и форма элемента, а форма элемента позволяет родителю передавать пропы вроде лимита элементов.',
          '- Эта стрелка отключается только при достижении лимита и видимости последнего элемента — до лимита достижение конца означает, что элементов будет больше.',
          '- `newItemsLimit` останавливает это демо на 24 элементах; в реальном коде эквивалентный сигнал — исчерпание страниц вашего API.',
        ].join('\n'),
      },
    ],
  },
  'custom-transition': {
    meta: {
      title: 'Кастомная анимация прокрутки в React: easing и длительность',
      description:
        'Кастомный easing и длительность для программной прокрутки в React: transitionBehavior передаёт целевую позицию, а вы анимируете scrollLeft. Живая демонстрация и исходник.',
    },
    title: 'Кастомная анимация прокрутки: собственные easing и длительность',
    lede: 'Нативная плавная прокрутка даёт одну скорость и одну кривую, выбранные браузером. Когда программная прокрутка должна соответствовать остальному дизайну движения, `noPolyfill={false}` позволяет взять управление — меню вычисляет, куда должен ехать рельс, а ваш код ведёт туда `scrollLeft`.',
    demoHint:
      'Кликайте стрелки и переключайте длительность — при 2500 мс кривую ease-in-out-cubic легко увидеть. Клик посреди анимации отменяет предыдущую.',
    prose: [
      {
        heading: 'Как это работает',
        body: 'По умолчанию меню прокручивает нативным `scrollIntoView` и игнорирует оба пропа переходов. Установка `noPolyfill={false}` направляет программную прокрутку через полифил scroll-into-view-if-needed, который вычисляет цель и передаёт её вашему `transitionBehavior` как инструкции: по одному действию `{ el, top, left }` на каждого прокручиваемого предка, которому нужно двигаться, — здесь всегда только контейнер прокрутки, потому что меню передаёт его как границу. Дальше `animateScroll` шагает `el.scrollLeft` к цели на каждом `requestAnimationFrame`, маппя прогресс через `easeInOutCubic` на выбранной длительности.',
      },
      {
        heading: 'Прерывание анимации на лету',
        body: 'Второй клик по стрелке может прийтись на середину анимации. История держит отложенный кадр на элемент в `WeakMap`, поэтому новый вызов отменяет старый цикл `requestAnimationFrame`, а не даёт двум из них бороться за `scrollLeft`. А поскольку каждая анимация читает свою начальную точку из текущего `scrollLeft` элемента, новая подхватывает ровно там, где остановилась прерванная.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Ничто здесь не привязано к функции easing — как только есть целевая позиция, работает любая кривая или библиотека анимаций.',
          '- Типизация описывает `transitionBehavior` как строку `ScrollBehavior`, но значение уходит напрямую в scroll-into-view-if-needed как его колбэк `behavior` — отсюда приведение типа в исходнике.',
          '- История связывает одно и то же состояние длительности и в `transitionDuration`, и в саму анимацию, чтобы они не расходились.',
        ].join('\n'),
      },
    ],
  },

  'prevent-body-scroll': {
    meta: {
      title:
        'Заблокировать прокрутку страницы колесом: горизонтальное меню React',
      description:
        'Прокручивайте горизонтальное меню React колесом мыши, пока страница стоит на месте: нативный непассивный слушатель колеса, включаемый по наведению. Живая демонстрация и полный исходник.',
    },
    title: 'Прокрутить меню колесом — не прокручивая страницу',
    lede: 'Горизонтальное меню под колесом мыши неудобно: колесо прокручивает страницу, а ряд стоит. Исправление из двух половин — обработчик `onWheel`, превращающий тики колеса в листание, и нативный непассивный слушатель, не дающий странице двигаться под ним. Вторую половину одним React не сделать.',
    demoHint:
      'Поставьте указатель на ряд и покрутите колесо: ряд листается, страница стоит. Уберите с ряда — и колесо снова прокручивает страницу.',
    prose: [
      {
        heading: 'Превращение колеса в листание',
        body: 'Проп `onWheel` у `ScrollMenu` вызывается с объектом API и событием колеса. Настоящее колесо мыши сообщает дельты только по Y грубыми шагами, поэтому обработчик вызывает `scrollNext`, когда `deltaY` отрицательна, и `scrollPrev` иначе — каждый тик листает ряд. Перед этим он проверяет, не похоже ли событие на жест тачпада: любая `deltaX` вообще или `deltaY` меньше 15.',
      },
      {
        heading: 'Почему блокировке страницы нужен нативный слушатель',
        body: "Вызов `preventDefault` внутри React-обработчика был бы очевидным способом остановить страницу — и он тихо ничего не делает, потому что React регистрирует слушатели колеса как пассивные, а пассивному слушателю запрещено отменять событие. Поэтому `usePreventBodyScroll` идёт в обход React: на `mouseenter` выполняет `document.addEventListener('wheel', preventDefault, { passive: false })`, на `mouseleave` снова снимает слушатель. Пока указатель над меню, каждое событие колеса всплывает до `document`, и там его действие по умолчанию — прокрутка страницы — отменяется. Очистка `useEffect` вызывает `enableScroll` при размонтировании, так что страница не может остаться заблокированной.",
      },
      {
        heading: 'Аварийный выход для тачпада',
        body: 'Двухпальцевый панораминг тоже приходит как события колеса, и контейнер прокручивается от них нативно — document-слушатель это убил бы. Для событий, подходящих под эвристику тачпада, обработчик вызывает `stopPropagation` и возвращается: событие не доходит до document-слушателя, и нативный панораминг выживает. Надёжного способа обнаружить тачпад нет; эвристика по дельте — честная догадка истории, и на практике она держится.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Браузеры сделали слушатели колеса на уровне document пассивными по умолчанию именно затем, чтобы страницы не подтормаживали прокрутку — `passive: false` это явный отказ, снова делающий `preventDefault` законным.',
          '- Колесо вверх листает вперёд, колесо вниз — назад: это маппинг истории; поменяйте ветки `scrollNext` / `scrollPrev` местами для обратного.',
          '- Сенсорные устройства не выполняют ничего из этого: `mouseenter` нет, а свайп по ряду с самого начала нативная прокрутка.',
          '- Блокировка существует только между `mouseenter` и `mouseleave`, поэтому остальная страница прокручивается нормально, как только указатель покидает рельс.',
        ].join('\n'),
      },
    ],
  },

  'one-item-scroll': {
    meta: {
      title: 'Прокрутка по одному элементу в React: точные стрелки карусели',
      description:
        'Продвигайте карусель React на один элемент за клик по стрелке: scrollToItem с getNextElement шагает одной карточкой вместо целой страницы. Живая демонстрация и полный исходник.',
    },
    title: 'Прокрутка по одному элементу вместо целой страницы',
    lede: 'По умолчанию стрелки листают: всё видимое выезжает, а следующая группа въезжает. Этот пример переподключает их на шаг — одна карточка за клик — и всё изменение состоит в том, что вызывает `onClick` стрелки. То же меню, те же элементы, другая цель прокрутки.',
    demoHint:
      'Кликните стрелку — ряд продвинется на одну карточку, а не на страницу. Стрелки отключаются на краях.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`getNextElement()` возвращает первый элемент за видимой группой; `getPrevElement()` — тот, что прямо перед ней. Правая стрелка вызывает `scrollToItem(visibility.getNextElement(), ’smooth’, ’end’)` — выравнивание этого элемента по конечному краю контейнера прокручивает ровно настолько, чтобы ввести его в видимость, что двигает ряд ровно на одну карточку. Левая стрелка — её зеркало: предыдущий элемент, выровненный по `’start’`.',
      },
      {
        heading: 'Выравнивание — весь трюк',
        body: 'Стандартный `scrollNext()` внутренне находит тот же следующий элемент, но выравнивает его по начальному краю — экран прокручивается мимо всей видимой группы, чтобы поставить этот элемент первым. Один аргумент `ScrollLogicalPosition` — разница между листанием и шагом. Третий параметр `scrollToItem` — стандартное выравнивание `inline` scroll-into-view; второй — поведение, здесь `’smooth’`.',
      },
      {
        heading: 'Примечания',
        body: [
          '- Состояние стрелок использует сокращения `’first’` и `’last’`: `useIsVisible(’first’, true)` отключает левую стрелку на старте, `useIsVisible(’last’, false)` правую в конце.',
          '- На краях `getNextElement()` возвращает undefined, и `scrollToItem` тихо ничего не делает, так что включённая стрелка всё равно не может перепрокрутить.',
          '- Обработчик `onWheel` истории по-прежнему листает полный экран за насечку колеса — шаг это поведение стрелок, а не глобальный режим.',
          '- Клики по элементам не тронуты: карточки переключают выбор через собственный `onClick`, независимо от того, как прокручивают стрелки.',
        ].join('\n'),
      },
    ],
  },

  'items-animation': {
    meta: {
      title: 'Анимация добавления и удаления элементов списка в React',
      description:
        'Добавляйте, удаляйте и перемешивайте элементы горизонтального списка React, анимируя через @formkit/auto-animate посредством пропа containerRef у ScrollMenu. Живая демонстрация и полный исходник.',
    },
    title:
      'Анимировать появление, исчезновение и место элементов с auto-animate',
    lede: 'Добавление в горизонтальный список заставляет новый элемент выскочить на место; удаление смыкает его соседей. `@formkit/auto-animate` чинит и то и другое одним родительским ref — а проп `containerRef` у `ScrollMenu` передаёт ему ровно тот элемент, который нужен.',
    demoHint:
      'Добавляйте, удаляйте и перемешивайте — каждое появление, исчезновение и перестановка анимированы. В самом меню нет кода анимации.',
    prose: [
      {
        heading: 'Как это работает',
        body: '`useAutoAnimate()` возвращает ref, который должен лечь на прямого родителя элементов, которые следует анимировать. Внутри `ScrollMenu` этот родитель — контейнер прокрутки: каждый переданный ребёнок оборачивается в div элемента, и эти div-элементы являются непосредственными детьми контейнера. История пропускает ref насквозь — `<ScrollMenu containerRef={parent}>` — и auto-animate берёт дальше на себя: добавленные элементы плавно въезжают, удалённые анимированно выезжают, а переставленные скользят на новое место. Само меню никогда не знает, что его анимируют.',
      },
      {
        heading: 'Добавление, удаление, перемешивание',
        body: 'Все три контрола — обычные вызовы `setState` над массивом items — `addItems` добавляет один, `removeItems` отбрасывает последний, `shuffle` — проход Фишера–Йетса по копии. Анимации полностью происходят из DOM-мутаций, вызванных этими обновлениями. Стоит держать одно правило: `itemId` служит и ключом React, и дескриптором элемента в карте отслеживания меню, поэтому id должны оставаться уникальными — история даже восполняет пробелы в нумерации, оставленные удалениями, вместо риска создать дубликат.',
      },
      {
        heading: 'Прокрутка и отслеживание продолжают работать',
        body: 'Меню заново наблюдает за своими детьми при каждом их изменении, поэтому `useIsVisible` только что добавленного элемента сразу сообщает корректно, а стрелки продолжают листать. Новый элемент, правда, обычно приземляется вне экрана — если появление должно быть реально видно, скомбинируйте это с `scrollToItem`, как делает пример add-item-and-scroll-to-it.',
      },
      {
        heading: 'Примечания',
        body: [
          '- `containerRef` принимает объект ref или колбэк-ref — колбэк `useAutoAnimate` вставляется напрямую.',
          '- auto-animate — без конфигурации и не зависит от фреймворка; привязка к React — один хук `useAutoAnimate`.',
          '- Демо выше упрощает управление id до монотонного счётчика; панель кода показывает версию истории с восполнением пробелов.',
        ].join('\n'),
      },
    ],
  },
};

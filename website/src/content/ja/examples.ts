// Japanese (ja) — translation of en/examples.ts.
//
// Values only: every key, its order and its type come from the English
// module, and a missing or renamed one is a type error rather than a
// silently English page.
// i18n:meta locale=ja source=en/examples.ts source-blob=60d5f83e262100978eb4d1dc9565659367d156c4 status=translated
import type { ExamplesCopy } from '../types.ts';

/** Copy for the example pages, keyed by the slugs in `examples-manifest.ts`. */
export const examples: ExamplesCopy = {
  'add-item-and-scroll-to-it': {
    meta: {
      title: 'React フィルターチップ：項目を追加してスクロール',
      description:
        'React 横スクロールのフィルターチップ：項目を追加し、レンダリング後に apiRef と scrollToItem でスクロール。ライブデモと完全なソース付き。',
    },
    title: '項目を追加してスクロール——フィルターチップのパターン',
    lede: 'ユーザーがフィルターを選ぶとチップバーは伸び、新しいチップは右端の外に隠れず画面上に現れるべきです。落とし穴は、まだレンダリングされていない要素へはスクロールできないこと。この例では、クリックハンドラーと副作用に作業を分けます。',
    demoHint:
      'フィルターを追加をクリック——チップが末尾に現れ、行がスクロールして表示します。x でチップを削除します。',
    prose: [
      {
        heading: '仕組み',
        body: 'メニューは `apiRef` を受け取り、コンポーネントツリーの外に完全な API を公開します。`addItem` は 2 つのことを行います。新しい id を `lastAdded` ref に保存し、項目を state に追加します。ここで意図的にスクロールしません——その時点ではチップは state にすぎず、DOM ではないからです。',
      },
      {
        heading: 'スクロールを副作用に置く理由',
        body: '`getItemElementById` は DOM で項目を探すので、スクロールは React が新しい項目をコミットした後にしか行えません。`items` をキーにした `useEffect` がまさにその時点で実行され、`lastAdded` を読み取ってクリアし、`apiRef.current.scrollToItem(el, ’smooth’, ’end’)` を呼びます。ref のクリアは重要です——他の理由（選択、矢印）による再レンダリングも同じ副作用に到達するため、再度スクロールしてはいけません。',
      },
      {
        heading: '注意点',
        body: `
          - \`lastAdded\` は state ではなく ref です。書き込み自体がレンダリングを引き起こしてはならず、値は直後の副作用実行にしか意味を持ちません。
          - \`’end’\` は新しいチップを行の右端に揃えます。中央に置きたい場合は \`’center’\` も同じように動きます。
          - ここでの矢印は \`useLeftArrowVisible()\` と \`useRightArrowVisible()\` を使います——\`useIsVisible(’first’/’last’)\` ペアの短縮形です。
          - スクロールバーはライブラリの \`scroll-container\` クラスに対するプレーンな CSS で隠しています。スクロール自体はネイティブのままです。
        `,
      },
    ],
  },

  'bottom-arrows': {
    meta: {
      title: 'メニューの下のカルーセル矢印：React でのカスタム配置',
      description:
        'React でカルーセル矢印を行の下に置く：ScrollMenu の Footer プロパティがメニューの下に任意のレイアウト（矢印含む）を描画。ライブデモと完全なソース付き。',
    },
    title: '矢印をメニューの下に——またはレイアウトのどこにでも',
    lede: '矢印は内蔵の装飾ではなく、あなたが渡すコンポーネントです。したがって配置はライブラリの設定ではなくレイアウトの判断です。この例では `LeftArrow` も `RightArrow` も渡さず、行の下の `Footer` スロットに、普通のコンテンツの隣に両ボタンを描画します。',
    demoHint:
      '矢印は行の下にあります——同じ VisibilityContext を読むので、端では依然として無効になります。',
    prose: [
      {
        heading: '仕組み',
        body: '`ScrollMenu` は `Footer` コンポーネントを受け取り、スクロールコンテナの下、項目と同じ `VisibilityContext.Provider` 内に描画します。このストーリーのフッターは、テキストと 2 つの矢印ボタンを保持する普通の flex div です。コンテキストが届くので、各ボタンは `React.useContext(VisibilityContext)` を呼び、サイドスロットで得るのとまったく同じ API を受け取ります——矢印自体は何も変わりません。',
      },
      {
        heading: '矢印の状態はいつもどおり',
        body: '`useLeftArrowVisible()` と `useRightArrowVisible()` は、行が既にその端にあるかを報告します。ストーリーは結果を `disabled` にマップし、ボタンをフェードアウトします。クリックは `scrollPrev()` と `scrollNext()` を呼びます。これらはボタンがどこにマウントされているかを知りませんし、気にもしません。',
      },
      {
        heading: '注意点',
        body: `
          - \`Header\` は行の上の鏡像スロットで、契約は同じです。
          - サイドの \`LeftArrow\`/\`RightArrow\` プロパティは、事前配置されたバリアントにすぎません——同じ矢印コンポーネントがどちらの位置でも機能します。
          - フッターは矢印専用ではありません。\`VisibilityContext\` を読むコンポーネントなら、そこでも完全な API を得られます。
          - ストーリーの \`onWheel\` ハンドラーはマウスホイールでページングし、タッチパッドのジェスチャーはネイティブスクロールに任せます。
        `,
      },
    ],
  },

  autoplay: {
    meta: {
      title: 'アクセシブルな一時停止を備えた React カルーセル自動再生',
      description:
        'React スクロールメニューの自動再生：useInterval が apiRef 経由で scrollNext を呼び、ホバー、フォーカス、タッチ、動きの抑制時に一時停止。ライブデモと完全なソース付き。',
    },
    title: 'アクセシブルな一時停止を備えた自動再生',
    lede: '進める部分は 1 行です——`apiRef` 経由で `scrollNext()` を呼ぶタイマーで、同じ無限ループコアの上に載せます。エンジニアリングの本体は、いつ*進めない*かにあります。ホバー、タッチ、キーボードフォーカス、一時停止ボタン、非表示タブ、画面外のレール、動きの抑制の設定——すべてが、それぞれ異なる理由でタイマーを止めます。',
    demoHint:
      'ホバー、タッチ、またはレールに Tab で入ると一時停止します。一時停止ボタンは、再生を押すまで止めます。',
    prose: [
      {
        heading: '仕組み',
        body: '`useInterval(cb, active ? interval : null)` がスケジューラー全体です。`active` は 4 つのフラグ——ユーザー一時停止、ホバー一時停止、フォーカス一時停止、`prefers-reduced-motion`——を束ね、`null` を渡すとタイマーを完全に除去します。そのため再開時は、ポインターが離れた直後のサイクル途中で発火するのではなく、新鮮で完全な間隔が始まります。',
      },
      {
        heading: '実行を拒むティック',
        body: 'アクティブなタイマーでさえ、スクロール前にチェックします。ティックは `api.menuVisible.current` と `document.visibilityState` を読み、どちらかがノーならスキップします。非表示タブは IntersectionObserver を凍結するため、そこでスクロールすると盲目的に進み、テレポートの帳簿がずれます。ページの外へスクロールされたレールは、そもそも動くべきではありません。スキップされたティックはコストゼロで、次のティックが再チェックします。',
      },
      {
        heading: '一時停止の表面',
        body: 'ホバーとタッチはラッパーハンドラーで、キーボードフォーカスは `onFocusCapture`/`onBlurCapture` で、そして `prefers-reduced-motion` は自動再生を完全にオフに保ちます。明示的な一時停止ボタンこそ、自動進行コンテンツに対して WCAG 2.2.2 が実際に要求するものです——ホバーでの一時停止だけでは数えられません。',
      },
      {
        heading: '注意点',
        body: `
          - 一時停止トグルはホバーラッパーの外に置きます——内側では、一時停止のクリックがホバー一時停止も引き起こし、ボタンが何かをしているようには決して観測できません。
          - ループは無限ループ例と同じ \`useInfiniteLoop\` クローン＆テレポートフックから来ます。自動再生が加えるのはタイマーと一時停止フラグだけです。
          - スクロールアニメーションはブラウザのネイティブスムーズスクロールです——デフォルトの \`noPolyfill\` では \`transitionDuration\` は効きません。
        `,
      },
    ],
  },

  'mouse-drag': {
    meta: {
      title: 'React ドラッグスクロール：クリックを壊さない横メニュー',
      description:
        'React 横リストのマウスドラッグスクロール：5px の閾値がドラッグとクリックを分け、項目のクリック可能性を保つ。ライブデモと完全なソース付き。',
    },
    title: 'マウスでドラッグしてスクロール——クリックを壊さずに',
    lede: 'タッチユーザーは横リストをネイティブにスクロールしますが、マウスユーザーには配線が必要です。押し、ドラッグし、離す。難しいのは行を動かすことではありません。素朴な実装が、ドラッグを離すたびに偶発的な項目クリックへ変えてしまうことです。この例では、小さな `DragDealer` クラスと 3 つのマウスプロパティで両者を分離します。',
    demoHint:
      '行のどこでも掴んでドラッグします。項目は依然としてクリック可能です——ドラッグ後のクリックは抑制されます。',
    prose: [
      {
        heading: '仕組み',
        body: '`ScrollMenu` はカリー化されたマウスハンドラーを公開します——`onMouseDown`、`onMouseUp`、`onMouseMove` はそれぞれ API オブジェクトを受け取り、通常のイベントハンドラーを返します。`DragDealer` インスタンスは 1 つのアンカー座標を追跡します。移動のたびに差分を直接 `scrollContainer.current.scrollLeft` に適用します。残りはネイティブスクロールが行います——トランスフォームも物理もなく、スクロールバーは本物のままです。',
      },
      {
        heading: 'クリックが機能し続ける理由',
        body: 'ドラッグはポインターが 5px を超えて動いた後でしか始まらないため、普通のクリックはスクロールしません。逆方向は古典的なバグです。項目の `onClick` は `mouseup` の後に発火するので、カードの上でドラッグを離すと選択してしまいます。`dragStop` は applying フラグを即座にクリアしますが、`dragging` はもう 1 アニメーションフレームだけ維持します——クリックハンドラーがそれを確認して中断します。',
      },
      {
        heading: '盗む価値のある詳細',
        body: `
          - \`dragStart\` は前のジェスチャーからの保留中のリセットをキャンセルします——これがないと、素早い 2 回目のドラッグが古い差分を適用し得ます。
          - ラッパーの \`onMouseLeave\` も \`dragStop\` を呼ぶため、ドラッグ途中で行を離れてもドラッグ状態に張り付きません。
          - タッチにはどれも不要です——コンテナは本物のスクロールコンテナなので、スワイプは最初から機能します。
        `,
      },
    ],
  },

  'save-restore-position': {
    meta: {
      title: 'React スクロール位置の保持：再マウントや戻るで復元',
      description:
        'onUpdate ごとにスクロールオフセットを sessionStorage に保存し、onInit で復元して、再マウントや再読み込みをまたいで位置を保持。ライブデモと完全なソース付き。',
    },
    title: 'スクロール位置の保存と復元',
    lede: '横レールはアンマウントのたびにオフセットを忘れます。ルートを離れて戻る、セクションを折りたたむ、すると先頭に跳ね戻ります。この例ではユーザーがスクロールするたびにオフセットを保存し、マウント時に書き戻して、メニューが離れた位置にぴったり戻るようにします。',
    demoHint:
      '行をどこかへスクロールし、メニューをアンマウントして、再びマウントする——レールは同じオフセットで戻ります。',
    prose: [
      {
        heading: '仕組み',
        body: '機能全体は 2 つのコールバックが担います。`onUpdate` はユーザーがスクロールして可視性状態が変わるたびに発火し、`savePos` は `api.scrollContainer.current.scrollLeft` を読み取って `sessionStorage` に書き込みます。次のマウントでは、`onInit` が保存値をそのまま `scrollLeft` に割り当てます——プレーンなプロパティ書き込みなので、復元はユーザーの前でアニメーションを再生するのではなく即時です。',
      },
      {
        heading: '再マウント、再読み込み、戻るナビゲーションを生き延びる',
        body: '`sessionStorage` はコンポーネントより長生きします。クライアントサイドのルート変更、条件付きレンダリング、ページ全体の再読み込みは、すべて保存されたオフセットに戻り、値はタブごとなので 2 つのタブが互いを上書きしません。履歴ナビゲーション用に、ストーリーは `window.history.scrollRestoration = ’manual’` も設定し、戻る/進むでブラウザ自身のスクロール復元が手動の復元と競合しないようにします。',
      },
      {
        heading: '注意点',
        body: `
          - 生の \`scrollLeft\` による復元はピクセル単位で正確で、どの項目が存在するかを気にしません——覚えるべき id も、調べるものもありません。
          - ストーリーの再読み込みボタンはメニューの \`key\` を差し替えて強制的に再マウントします。デモのアンマウント/再マウントトグルは、同じテストを明示したものです。
          - リセットはストレージキーを削除するだけです——次のマウントは初回訪問のようにゼロから始まります。
        `,
      },
    ],
  },

  'one-item': {
    meta: {
      title: 'React 1 画面 1 項目のスライダー：全幅スクロール項目',
      description:
        'React 横スクロールメニューの全幅項目：項目ラッパーへの min-width 100% が、1 画面 1 項目のスライダーを作る。ライブデモと完全なソース付き。',
    },
    title: '1 画面に 1 項目：同じメニューからの全幅スライダー',
    lede: '切り替えるスライダーモードはありません。メニューは CSS の指示どおりにレイアウトするので、1 つのルール——ライブラリの項目ラッパーへの `min-width: 100%`——が同じコンポーネントをスライダーに変えます。各カードが画面を満たし、通常のページング矢印がちょうど 1 項目ずつ進めます。',
    demoHint:
      '矢印でページング——各スライドはちょうど 1 画面幅で、各スライドが自身の可視性を報告します。',
    prose: [
      {
        heading: '仕組み',
        body: 'ストーリーはメニューを、`.react-horizontal-scrolling-menu--item`——ライブラリが各子の周りに描画する div——を対象にしたスタイル付きコンテナで包み、`minWidth: ’100%’` と flex 中央揃えを与えます。各ラッパーがスクロールコンテナ全体に広がるので、1 枚のカードが画面に収まります。矢印は標準のものです。`scrollPrev()` と `scrollNext()` は可視グループごとにページングし、可視グループが 1 項目のとき、ページと項目は同じものです。',
      },
      {
        heading: '矢印とホイール',
        body: '矢印状態は `useLeftArrowVisible()` と `useRightArrowVisible()` から来ます——行がその端にあるとそれぞれ true を返し、ストーリーは `disabled` に流し込んでボタンをフェードアウトします。`onWheel` プロパティはイベントとともに API オブジェクトを受け取るので、縦のマウスホイールが `deltaY` の符号で行をページングします。まずタッチパッドを嗅ぎ分けます。水平差分、または 15 未満の縦差分はタッチパッドジェスチャーとみなし、ネイティブスクロールに任せます。',
      },
      {
        heading: '注意点',
        body: [
          '- 各子の `itemId` が唯一のハード要件です——項目はこれで追跡され、スクロール先になります。',
          '- カードは依然として `useIsVisible(itemId, true)` を呼びます。1 画面 1 項目では、画面外のスライドはすべて `visible: false` を報告します。',
          '- スクロールバーはスクロールコンテナ上のプレーンな CSS（`scrollbar-width: none` と WebKit 擬似要素）で隠します——その選択はあなたのもので、ライブラリのものではありません。',
          '- 幅は完全にあなたのスタイルシートにあります。100% を 50% に替えれば 2 画面 1 項目のスライダーです。ライブラリは何も計測しません。',
        ].join('\n'),
      },
    ],
  },

  performance: {
    meta: {
      title: 'React 横リストのパフォーマンス：5000 項目',
      description:
        'ネイティブスクロールで 5000 項目を描画する React 横メニュー：メモ化カード、1 つの IntersectionObserver、仮想化なし。ライブデモと完全なソース付き。',
    },
    title: '1 行に 5000 項目——仮想化は不要',
    lede: '数百項目での定石は仮想化です。この例は 5000 個の実 DOM ノードを 1 つの `ScrollMenu` に描画し、応答性を保ちます——ネイティブの overflow スクロールが移動を、IntersectionObserver が監視を担い、React はほぼ何もしません。',
    demoHint:
      'レールをドラッグするか矢印でページング——5000 枚のカードすべてが実 DOM ノードです。ウィンドウ化はされていません。',
    prose: [
      {
        heading: '仕事が起きない場所',
        body: 'スクロールは決して React に入りません。レールは本物の overflow コンテナです。ホイールとタッチはネイティブにスクロールし、ドラッグ配線は `scrollContainer.current.scrollLeft` への代入だけです——state も、フレームごとの再レンダリングもありません。可視性は、5000 個の項目要素すべてを監視する単一の IntersectionObserver インスタンスです。コールバックはバッチで到着し、`useIsVisible` で購読したコンポーネントだけが、自身の項目が反転したときに更新します。どこにも項目ごとのスクロール計算はありません。',
      },
      {
        heading: 'ストーリーが調整しているもの',
        body: '`Card` は `selected` と `title` を比較するコンパレータ付きの `React.memo` で包まれ、1 枚のカードを選択しても残り 4999 枚を調整しません。可視性表示は `useDeferredValue` を通ります。ページジャンプ後は数百の項目が一度に状態を反転しますが、遅延させることで、その原因となった操作のクリティカルパスからそのバーストを外します。`noPolyfill={true}` は、プログラムによるスクロールをスムーズスクロールポリフィルではなくブラウザ自身の `scrollIntoView` にします。ドラッグは mouse-drag 例と同じ `DragDealer` パターンです。',
      },
      {
        heading: 'このページが認めるトレードオフ',
        body: '上のデモレールはサーバーレンダリングされません。5000 枚のカードはおよそ 1 メガバイトの HTML にシリアライズされるため、レールは高さ一致のプレースホルダーの背後でクライアントのみでマウントされ、レイアウトシフトはありません。それがこの規模の本当の代償です——ブラウザは 5000 個のライブノードを楽々扱いますが、SSR ペイロードとして送るのは別の判断です。数万ノードのどこかで、メモリと初期レンダリングのコストも追いつきます。そこからウィンドウ化は任意ではなくなります。',
      },
      {
        heading: '注意点',
        body: [
          '- 5000 枚のカードの DOM は、マウント時に一度だけ構築されます——`React.memo` が以後の親レンダリングを各カードの no-op に変えます。',
          '- 矢印はおよそ 1 ビューポートずつページングするため、矢印だけでレール全体を横断するのは設計上遅いです——ドラッグのフリックや `scrollToItem` ジャンプがこの規模には合います。',
          "- 矢印は依然として `useIsVisible('first')` と `useIsVisible('last')` で動きます——10 項目のメニューと同じ観測機構を、500 倍の項目数で。",
        ].join('\n'),
      },
    ],
  },

  progress: {
    meta: {
      title: 'カルーセルの React 横スクロール進捗インジケーター',
      description:
        'React 横メニューの進捗バー：onUpdate を購読し、可視項目を数え、現在ページを導出。ライブデモと完全なストーリーソース付き。',
    },
    title: '横メニューにスクロール進捗インジケーターを追加',
    lede: 'スクロールバーを隠したカルーセルも、「あとどれくらい？」の答えをユーザーに負っています。メニューは既に知っています。全項目の可視性を追跡しているので、位置は数えるだけです。ストーリーはそのデータから番号付きページボタンと左右の残り項目数を描画します。このデモは同じ計算を進捗バーへ凝縮します。',
    demoHint:
      '行をスクロール、ドラッグ、または矢印で——バーがページごとに満ち、カウンターが現在位置を示します。',
    prose: [
      {
        heading: '仕組み',
        body: 'インジケーターは `Footer` プロパティとして渡され、`ScrollMenu` は `VisibilityContext` が利用できるメニュー内部にそれを描画します。コンテキストから `items`——可視性追跡の背後にあるマップ——を取り、`items.subscribe(’onUpdate’, cb)` で購読します。そのイベントは IntersectionObserver のコールバックごとに発火するので、ストーリーは `items.getVisible()` を読む前にデバウンス（タイムアウトと `requestAnimationFrame`）します。',
      },
      {
        heading: '可視項目からページ番号へ',
        body: '可視項目の数がページサイズです。総ページ数は `Math.ceil(items.size / visibleItemsLen)`、現在ページは最後の可視エントリの `index` から来ます。ストーリーはそれをクリック可能なページボタンに変え——それぞれが `scrollToItem(getItemByIndex(itemInd))` を呼び、id を知らずに位置で項目を指定し——同じ数値から左側と右側の項目数を導出します。デモのバーは `currentPage / totalPages` を幅パーセントにしただけです。',
      },
      {
        heading: '注意点',
        body: [
          '- ピクセルで測るものはありません——計算は完全に可視性データの上で動くので、項目幅が異なっても機能し続けます。',
          '- ビューポートをリサイズするとページサイズも従います。より多くの項目が収まり、`getVisible()` がより多くのエントリを返し、ページ数は次の更新で再計算されます。',
          '- 副作用は `items.unsubscribe` を呼び、保留中のタイマーをクリアするクリーンアップを返します——省略すると、アンマウントされたフッターが呼ばれ続けます。',
          '- 最初の観測報告の前は `getVisible()` は空です。ストーリーはそれまで `null` を返し、デモは空のトラックを描きます。',
        ].join('\n'),
      },
    ],
  },

  'scroll-to-item': {
    meta: {
      title: 'React 横リストで要素へスクロール：scrollToItem',
      description:
        'id で React 横リストを任意の項目へスクロール：onInit が api を渡し、scrollToItem が対象をビューへ。ライブデモと完全なソース付き。',
    },
    title: '横リストの特定の項目へスクロール',
    lede: '行へのディープリンク。チャットは進行中の会話に、ギャラリーは共有した写真に開きます。スクロールコンテナはライブラリ内部にありますが、その DOM への ref は不要です——`onInit` が api を渡し、`scrollToItem` が位置決めを行います。',
    demoHint:
      'レールは Tokyo でマウントしません——onInit が直接 quito へジャンプします。別の場所へドラッグし、再マウントして再びそこに着地するのを見てください。',
    prose: [
      {
        heading: '仕組み',
        body: '`ScrollMenu` は `onInit` コールバックを受け取り、メニューが描画して項目を計測した後に、内部の `VisibilityContext` が提供するのと同じ api オブジェクトを渡して呼びます。ハンドラーは `getItemElementById(id)` で要素を探し、`scrollToItem(item, ’auto’, ’start’)` に渡します。`onInit` は計測後にしか発火しないため、描画済み項目の検索が空になることはありません——`setTimeout` もリトライループも不要です。',
      },
      {
        heading: '動作とアラインメント',
        body: 'ストーリーは `’auto’` と `’start’` を渡します。`’auto’` はアニメーションなしでジャンプします。初期位置に欲しいのはそれです——ユーザーがレールを最初の項目で見ることはありません。`’start’` は項目の左端をレールに揃えます。クリック駆動のスクロールでは、同じ呼び出しが `’smooth’` と `’center’` を取ります——それが下のクリック中央揃えの例です。',
      },
      {
        heading: '注意点',
        body: [
          '- スロットは知っているが id を知らない場合、`getItemElementByIndex` が位置による代替です。',
          '- 渡す id は項目の `itemId`——可視性追跡にメニューが使うのと同じキーです。',
          '- デモは新しい `key` でメニューを再マウントして動作を再生します。新規マウントは毎回 `onInit` を実行します。',
        ].join('\n'),
      },
    ],
  },
  'center-on-click': {
    meta: {
      title: 'React スクロール可能タブ：クリックでアクティブタブを中央に',
      description:
        'Material UI なしの React スクロール可能タブ：タブをクリックすると scrollToItem(el, "smooth", "center") で中央揃え。ライブデモと完全なストーリーソース付き。',
    },
    title: 'クリックした項目を中央に——スクロール可能タブのパターン',
    lede: 'すべてのタブストリップが必要とし、どのスクロールコンテナも無料では与えてくれない動作。端の近くのタブをクリックすると中央へ滑り、両隣を見せます。ここでは API 呼び出し 1 つ——Material UI も、計測も、スクロール計算も不要です。',
    demoHint:
      'どちらかの端近くのタブをクリック——アクティブになり、行の中央に自分を揃えます。',
    prose: [
      {
        heading: '仕組み',
        body: '`handleItemClick` はカリー化されています。`itemId` を受け取り、API オブジェクトを期待する関数を返します。クリックはまず id を `selected` state に保存し、次に `api.getItemElementById(itemId)` で実 DOM 要素を見つけ、`api.scrollToItem(item, ’smooth’, ’center’)` に渡します。1 クリック、2 つの効果：タブが選択され、中央に揃います。',
      },
      {
        heading: 'API はどこから来るか',
        body: '親コンポーネントは API ref を決して保持しません。各 `Card` は `VisibilityContext`——`ScrollMenu` のどの子からも利用可能——から完全な API を読み、クリックハンドラーへ渡します：`onClick(visibility)`。代わりにメニューの外からスクロールする必要があるなら、それが scroll-to-item 例の `apiRef` パターンです。',
      },
      {
        heading: '注意点',
        body: [
          '- `scrollToItem` の第 3 引数は、`scrollIntoView` の `inline` オプションと同じ値を取ります——`’start’`、`’center’`、`’end’`。',
          '- カードはフォーカス可能で（`role="button"`、`tabIndex=0`）、`onKeyDown` で Enter を処理するため、キーボードユーザーにも同じ選択と中央揃えが得られます。',
          '- `onWheel` ハンドラーはマウスホイールの差分を `scrollNext`/`scrollPrev` にマップしますが、タッチパッドでは中断します——水平差分や極小の縦差分はジェスチャーとみなしネイティブのままにします。',
          '- 矢印は `useIsVisible(’first’)` と `useIsVisible(’last’)` の短縮形で自身を無効化します。',
        ].join('\n'),
      },
    ],
  },

  'swipe-desktop': {
    meta: {
      title:
        'デスクトップでマウススワイプ：React カルーセルのフリックジェスチャー',
      description:
        'React 横メニューのデスクトップスワイプ：マウスの押下/解放を追跡し、50px 超の解放がスムーズな滑りで次のページへフリック。デモと完全なソース付き。',
    },
    title: 'デスクトップでスワイプ：メニューをページングするマウスフリック',
    lede: 'ドラッグスクロールは行をカーソルと 1:1 で動かします。これはもう一方のマウスジェスチャー、フリックです。押し、少なくとも 50px 動かし、離す——メニューは `scrollNext` か `scrollPrev` でその方向に 1 ページ滑ります。行はポインターにまったく追従しません。滑りはライブラリのスムーズなプログラム的スクロールで、それが解放に慣性の感覚を与えます。',
    demoHint:
      '行のどこかを押し、左右に少なくとも 50px 動かして離す——メニューが 1 ページ滑ります。短い動きは何もしません。',
    prose: [
      {
        heading: '仕組み',
        body: '`useSwipe` フックは、`ScrollMenu` が期待する 3 つのカリー化マウスプロパティを返します——それぞれ API オブジェクトを受け取り、通常のイベントハンドラーを返します。`onMouseDown` はポインターの `clientX` を ref にアンカーし、`onMouseMove` は終了座標を上書きし続け、`onMouseUp` は両者を比較します。`minSwipeDistance`（50px）を超える水平差分なら、左方向のフリックで `apiObj.scrollNext()`、右方向で `apiObj.scrollPrev()` を呼びます。',
      },
      {
        heading: 'クリックに特別な処理が要らない理由',
        body: 'ドラッグスクロール例では、カードの上でドラッグを離すとクリックになるため、`dragging` フラグをジェスチャーより 1 フレーム長く生きさせる必要がありました。フリックは問題全体を回避します。50px 未満では `onMouseUp` は何もしないので、クリックはただのクリックです——そして超えればポインターは押したカードから既に離れています。フラグも、抑制されたハンドラーもありません。',
      },
      {
        heading: 'ストーリーがタッチとホイールに加えるもの',
        body: 'ストーリーはネイティブのタッチパンも固定します。React 18+ は `touchmove` リスナーをパッシブで登録するため、`preventDefault` は非パッシブリスナーからしか効きません。副作用が `apiRef`（`ref.current.scrollContainer.current`）を通じてスクロールコンテナへ到達し、`{ passive: false }` でリスナーを張ります。その `onWheel` ハンドラーもヒューリスティック付きでメニューをページングします——非ゼロの `deltaX` や小さな `deltaY` はタッチパッドとみなし、放っておきます。',
      },
      {
        heading: '注意点',
        body: [
          '- 座標は state ではなく ref にあります——state で `mousemove` を追跡するとピクセルごとに再レンダリングされます。',
          '- デモは `mousedown` で終了座標を再アンカーするため、前のジェスチャーの残り位置が新しいスワイプに数えられることは決してありません。',
          '- `minSwipeDistance` は好みで調整します。低いほど反応が速く、高いほど雑なクリックを許容します。このレシピのタッチ版は 20px を使います。',
        ].join('\n'),
      },
    ],
  },

  'mobile-swipe-only': {
    meta: {
      title: 'モバイルでカルーセル矢印を隠す：タッチのみの React スクロール',
      description:
        'React 横メニューで、デスクトップは矢印、モバイルはタッチのみのスクロール：pointer: coarse の matchMedia チェックが隠す。ライブデモと完全なソース付き。',
    },
    title: 'モバイルで矢印を隠す——小画面ではタッチのみのスクロール',
    lede: 'タッチ画面では矢印ボタンは死に荷物です。スワイプはネイティブ、親指がタップターゲットを覆い、各矢印が行の幅を食います。デモはマウスユーザーのために矢印を残し、ポインターが指のときはアンマウントします。ストーリーはさらに進み、ネイティブパンを明示的なスワイプでページングするジェスチャーに置き換えます。',
    demoHint:
      '携帯で開くか、DevTools でタッチエミュレーションを有効に——矢印が消え、スワイプがすべての仕事をします。',
    prose: [
      {
        heading: 'デモが矢印を隠す方法',
        body: '`LeftArrow` と `RightArrow` は任意のプロパティです——`undefined` を渡すとスロットは一切描画されず、CSS で隠すものも、タブ順序に残るボタンもありません。切り替えは副作用内の `matchMedia(’(pointer: coarse)’)` チェックです。サーバーはポインターの種類を知れないため、最初のペイントは矢印込みのデスクトップ優先で、粗いポインターが確認されるとハイドレーションがそれを削除します。`change` リスナーがライブに保ちます——DevTools のデバイスエミュレーションは再読み込みなしで切り替わります。',
      },
      {
        heading: 'ストーリーがタッチで行うこと',
        body: 'ストーリーの `useSwipe` フックは自由なパンをページングに変えます。カリー化された `onTouchStart`、`onTouchMove`、`onTouchEnd` プロパティはそれぞれ API オブジェクトを受け取ります。start は終了座標をリセットして `targetTouches[0].clientX` を記録し、move はそれを追跡し、end は移動距離を測ります。`minSwipeDistance`（20px）を超えると `apiObj.scrollPrev()` か `apiObj.scrollNext()` を呼びます——指の速さにかかわらず、スワイプごとにスムーズに 1 ページです。',
      },
      {
        heading: 'ネイティブのタッチスクロールの抑制',
        body: 'ページングだけを動きにするには、ブラウザ自身のパンを止める必要があります。React 18+ は `touchmove` リスナーをパッシブで登録し、そこでは `preventDefault` が無視されます。ストーリーの副作用は `apiRef`（`ref.current.scrollContainer.current`）を通じて実スクロール要素へ到達し、`{ passive: false }` で自身のリスナーを張ります。その場所では呼び出しが機能します。',
      },
      {
        heading: '注意点',
        body: [
          '- SSR のデフォルトは意図的に選びます。先に矢印を描画するのはクローラーとデスクトップユーザーに有利で、タッチデバイスはハイドレーション直後に失います。',
          '- `(pointer: coarse)` が対象とするのは入力であって画面サイズではありません——狭いデスクトップウィンドウは矢印を保ち、タブレットは失います。',
          '- 矢印を隠してネイティブスワイプを残したいだけなら（デモの動作）、ストーリーの `touchmove` 副作用を省きます——自由なパンと隠れた矢印は問題なく共存します。',
          '- タッチ閾値は 20px、デスクトップフリックの 50px に対してです——マウス版は swipe-on-desktop 例を参照してください。',
        ].join('\n'),
      },
    ],
  },

  'infinite-loop': {
    meta: {
      title: 'React 無限ループスクロールメニュー：シームレスなカルーセル',
      description:
        'カルーセルライブラリなしの React シームレスループカルーセル：両端へのクローンと、スクロール安定時の scrollLeft テレポート 1 回。デモと完全なソース付き。',
    },
    title: '公開 API の上に構築した無限ループメニュー',
    lede: '古典的なクローン＆テレポートのカルーセルのトリックを、ライブラリの変更ゼロで実装します。行を両端へクローンし、クローンゾーン内でスクロールが安定したとき、`scrollLeft` がちょうどループ 1 周分ジャンプします。ジャンプの両側のフレームは同一なので、何も動いたように見えません。矢印、ホイール、タッチ、マウスドラッグのすべてが継ぎ目を横断します。',
    demoHint:
      'どちらの方向にも進み続ける——矢印、ホイール、タッチ、ドラッグで——行は決して終わりません。',
    prose: [
      {
        heading: '仕組み',
        body: '`getSlides` は項目を行の両端へコピーします。`itemId` は一意でなければならないため、クローンにはサフィックスが付きます——左が `-lc`、右が `-rc`——一方、実 id はタイトル・選択・クリックのために `realId` として保持します。`useInfiniteLoop` が残りをまとめます。`normalize()` は最初の実項目とその右クローンの `offsetLeft` からループ長を測り、位置がクローンゾーンに入るたびに `scrollLeft` をちょうどその距離だけシフトします。純粋な幾何であり、冪等です——修正が不要なときに呼んでも何もしません。',
      },
      {
        heading: 'テレポートの発火タイミング',
        body: 'スクロール途中のジャンプはブラウザと目に見えて争うため、`normalize` はスクロールが安定したときに走ります。コンテナ上のネイティブ `scrollend` リスナー（`containerRef` プロパティ経由で到達）に、`scrollend` を発火しない Safari 向けの 150ms デバウンス `onScroll` フォールバックを添えます。誰かが何かを見る前にもう 1 回ジャンプします。レイアウト効果が初期 `scrollLeft` を最初の実項目にペイント前に設定し、ページが左クローンで開くことは決してありません。',
      },
      {
        heading: 'ドラッグ途中で継ぎ目を横断',
        body: 'マウスドラッグのコールバックは各差分を `scrollLeft` に加え、ジェスチャーの中で、まさにその場で `loop.normalize()` を呼びます。これがないと、クローンゾーンへのドラッグはテレポートまでドラッグの終了を待つことになります——あれば、継ぎ目を際限なくドラッグしても気づきません。',
      },
      {
        heading: '注意点',
        body: [
          '- ここでの矢印はカスタムで常に有効です。標準の `first`/`last` フックは最外部の項目を追跡しますが、ここではそれがクローンです——継ぎ目で無効がちらつきます。',
          '- カードは双和の可視性を表示します——項目は自身かどちらかのクローンが可視のときに可視と数えます。テレポート後、要素ごとのフラグは 1 フレーム古くなり、ヘッダーを点滅させるためです。',
          '- 片側 2 ページ分のクローン。ゾーンは全ビューポートを（ジャンプの周りで同一フレームを）余裕を持って覆う必要があり、継ぎ目にまたがるページからの Next クリックが行の端でクランプされることはありません。',
          '- ここで使うすべて——`containerRef`、`onScroll`、`itemId`、カリー化マウスプロパティ——は公開 API です。',
        ].join('\n'),
      },
    ],
  },

  simple: {
    meta: {
      title: 'React 横スクロールメニュー：入門例',
      description:
        '最小の react-horizontal-scrolling-menu 構成：itemId 付き項目、VisibilityContext を読む 2 つの矢印、項目ごとの可視性追跡。完全なソース付き。',
    },
    title: 'はじめに：React の横スクロールメニュー',
    lede: '最小の有用な構成：カードの行、2 つの矢印ボタン、そしてこのライブラリの本質——各カードが画面にいるかを知っていること。コンポーネント 1 つ、必須プロパティ 1 つ、スタイルシートのインポート 1 つ。',
    demoHint:
      '行をスクロール——矢印は端で無効になり、各カードが自身の可視性を追跡します。',
    prose: [
      {
        heading: '仕組み',
        body: '`ScrollMenu` は子要素をネイティブのスクロールコンテナ内に描画し、それぞれを IntersectionObserver で監視します。唯一の契約は `itemId`——各子の一意なプロパティで、項目の追跡・検索・スクロール先になります。どの子や矢印の中でも、`VisibilityContext` が完全な API を渡します。',
      },
      {
        heading: '可視性フック',
        body: 'カードは `useIsVisible(itemId)` を呼んで自身の在画状態を購読します——スクロールリスナーも位置計算もなく、可視性が変わったとき影響を受けたカードだけが再レンダリングされます。矢印は `first` と `last` の短縮形で行の両端で自身を無効化します。',
      },
      {
        heading: '注意点',
        body: [
          '- `styles.css` は別途インポートします——JS バンドルが CSS を注入することはありません。',
          '- 項目の幅はあなた自身の CSS。メニューは何も計測せず、210 バイトのレイアウトスタイルのみ同梱します。',
          '- `useIsVisible(itemId, true)` の第 2 引数は、観測器が報告する前の値——そしてメニューをサーバーレンダリングするなら、サーバーが描画する値です。',
        ].join('\n'),
      },
    ],
  },

  vertical: {
    meta: {
      title: '矢印付きの React 縦スクロールメニュー',
      description:
        'react-horizontal-scrolling-menu を縦に：flex-column スクロールコンテナ、有界の高さ、Header/Footer による上下の矢印。ライブデモとソース付き。',
    },
    title: '縦スクロールメニュー——同じコンポーネントを CSS で回す',
    lede: '`vertical` プロパティはありませんし、必要もありません。メニューはネイティブスクロールコンテナ内の flex 行なので、下向きにするのは数個の CSS オーバーライドです。可視性追跡、矢印フック、`scrollPrev`/`scrollNext` はすべて新しい軸で機能し続けます。',
    demoHint:
      '列の上でホイールを回すか矢印を使う——Up と Down は ScrollMenu の Header と Footer です。行はビューを離れると薄暗くなります。',
    prose: [
      {
        heading: '2 つのオーバーライドと 1 つの高さの境界',
        body: 'ストーリーはライブラリの 2 つのクラス名を再スタイルします。スクロールコンテナはデフォルトの `max-content` の代わりに `flex-direction: column`、`overflow-y: auto`、`height: initial` を得て、ラッパーは `height: 100%` を得るので、親の固定高さがそのままスクロール境界になります。それが縦モードのすべてです。ストーリーは emotion でオーバーライドを適用し、このページのデモは代わりに `wrapperClassName` と `scrollContainerClassName` プロパティで Tailwind ユーティリティを渡します——どのスタイル経路でも機能し、クラス名は安定しています。',
      },
      {
        heading: '矢印が Header と Footer になる',
        body: "`LeftArrow`/`RightArrow` スロットはレールの横に描画されます——列には不向きな位置です。`ScrollMenu` は上下に描画される `Header` と `Footer` コンポーネントも受け取り、ストーリーはそこに Up と Down ボタンをマウントします。それらは普通の `VisibilityContext` 消費者です。`useIsVisible('first', true)` が最上部で Up を無効にし、`useIsVisible('last', false)` が最下部で Down を無効にします。クリックは第 3 引数を渡します——`scrollPrev(undefined, undefined, 'end')` と `scrollNext(undefined, undefined, 'start')`——`scrollIntoView` の `block` 位置です。`'end'` は前の項目を下端に置き（丸々 1 ページ上へ）、`'start'` は次の項目を最上部に置きます（丸々 1 ページ下へ）。デフォルトの `'nearest'` では、各クリックは次の行をかろうじてビューへ押し込むだけです。",
      },
      {
        heading: 'スクロールを列の内側に保つ',
        body: "`scrollIntoView` は対象のスクロール可能な祖先をすべて動かし、ページもその 1 つです——したがって列内での `block` 整列ジャンプはドキュメント全体を巻き込みます。その連鎖を止めるのが `boundary` で、第 4 引数で渡します：`scrollNext(undefined, undefined, 'start', { boundary })` にメニュー自身の `scrollContainer.current` を渡すと、行だけをスクロールし他は動かしません。`boundary` を理解するのはポリフィルだけなので、`ScrollMenu` には `noPolyfill={false}` が必要です——上のデモは両方を渡しています。横メニューがこれに出くわすことは稀です。デフォルトの `block: 'nearest'` はそもそもページに縦移動を求めないからです。",
      },
      {
        heading: '可視性に軸はない',
        body: '`useIsVisible` は IntersectionObserver が支えており、交差は両次元で測られます——行は上下端を横切るとき、横項目が両側で行うのとまったく同じように状態を報告します。デモはビューの外の行を薄暗くしてそれを見せ、フックの `defaultValue` 引数で最初の 4 行をサーバーペイント済みの可視にしています。',
      },
      {
        heading: '注意点',
        body: [
          '- 固定される唯一の寸法はパネルのインライン高さです。ラッパーの `height: 100%` がそれをスクロールコンテナまで運びます。',
          '- ホイールとタッチは列をネイティブにスクロールします——`overflow-y: auto` が本物のスクロールコンテナにするからです。矢印は便宜であり、機構ではありません。',
          '- `scrollPrev`/`scrollNext` の第 2 引数は `inline`（水平）位置です——縦メニューが気にするのは `block` であり、ストーリーが明示的に渡す理由です。',
        ].join('\n'),
      },
    ],
  },

  rtl: {
    meta: {
      title: 'React 横スクロール RTL：右から左のメニュー',
      description:
        'React の右から左の横スクロールメニュー：RTL プロパティがスクロール方向とページングを反転し、矢印が側を入れ替える。ライブデモと完全なソース付き。',
    },
    title: '右から左の横メニュー',
    lede: 'アラビア語やヘブライ語のインターフェイスでは、行は右端から始まり左へ伸びる必要があります。1 つのブールプロパティがスクロールコンテナを反転します。残る本当の仕事は、「進む」が左を指すとき矢印が何を意味するかを決めることだけです。',
    demoHint:
      'スイッチを切り替え——行は反対の端から再始動し、矢印が役割を交換します。',
    prose: [
      {
        heading: '仕組み',
        body: '`RTL={true}` はスクロールコンテナを右から左モードにします。最初の項目が右端に座り、スクロールは左へ進みます。論理はすべて論理のままです——`useIsVisible(’first’)` は依然としてデータの最初の項目を意味し、`scrollNext()` は依然として最後へ向かいます——画面の方向だけが反転します。',
      },
      {
        heading: '矢印はスロットを交換し、論理は交換しない',
        body: '`LeftArrow` プロパティは常に画面の左側に描画されます。RTL ではその側が「進む」の居場所なので、ストーリーはスロットに入れ替えた要素を渡します：`LeftArrow={RTL ? <RightArrow /> : <LeftArrow />}`。コンポーネント自体は論理を保ちます——`scrollPrev` に配線された方は依然として `useIsVisible(’first’)` で無効になります——変わるのは画面位置とラベルだけです。',
      },
      {
        heading: '注意点',
        body: [
          '- ストーリーは `noPolyfill={true}` を渡すので、プログラム的スクロールはバンドルされたポリフィルではなくブラウザのネイティブスムーズスクロールを使います。',
          '- `scrollPrev(’smooth’, ’end’)` と `scrollNext(’smooth’, ’start’)` は明示的な整列を渡します——第 2 引数は `scrollToItem` が取るのと同じ `start/center/end` の組です。',
          '- ストーリーはチェックボックスから `RTL` をライブで切り替えます——プロパティはただの state で、メニューにビルド時に設定されるものは何もありません。',
        ].join('\n'),
      },
    ],
  },

  'add-items': {
    meta: {
      title: 'React 横無限スクロール：末尾でさらに読み込む',
      description:
        'React の横無限スクロール：onUpdate が api.items.last().visible を確認し、ローダー項目で次のバッチを追加。ライブデモと完全なソース付き。',
    },
    title: '末尾がビューに入ったら項目をさらに読み込む',
    lede: 'スクロールリスナーなしの横無限スクロール。メニューは既にどの項目が可視かを知っているので、「ユーザーが末尾に達したか」はただの質問です——最後の項目が画面にいるか？`onUpdate` がスクロールのたびに尋ね、答えがイエスのとき次のバッチを追加します。',
    demoHint:
      '右端へスクロール——ローダーカードが現れ、次のバッチが到着します。デモは 30 項目で止まります。',
    prose: [
      {
        heading: '仕組み',
        body: '`onUpdate` は項目の可視性が変わるたびに発火します。ハンドラーは `api.items.last()?.visible` を読みます——ライブラリは全項目を `itemId` で追跡し、項目ごとに可視性フラグを保つので、末尾の検出は 1 回のルックアップで済み、独自の IntersectionObserver もスクロール位置計算も不要です。`pushNewItems` はフェッチをシミュレートします。1 秒のタイムアウト、5 項目追加、以上です。',
      },
      {
        heading: 'フェッチを守る',
        body: '可視性の更新はバーストで到着するため、ハンドラーは繰り返し呼んでも安全でなければなりません。`loading` フラグが冪等にします。`onUpdate` と `pushNewItems` の両方がそれを確認し、最初のトリガーだけがフェッチを開始します。同じフラグが `Loader` コンポーネントを本物のメニュー項目（自身の `itemId` 付き）として描画し、マウント時に `scrollIntoView()` を呼んで、バッチの読み込み中に行の末尾をビューに保ちます。',
      },
      {
        heading: '注意点',
        body: [
          '- 右矢印は要素として渡されます。`RightArrow={<RightArrow disabled={...} />}`——コンポーネント形式と要素形式の両方が機能し、要素形式では親が項目上限のようなプロパティを渡せます。',
          '- その矢印は、上限に達して最後の項目が可視のときだけ無効になります——上限前は、末尾に達するということはさらに項目が来るということです。',
          '- `newItemsLimit` はこのデモを 24 項目で止めます。実際のコードでの相当する合図は、API がページを使い果たすことです。',
        ].join('\n'),
      },
    ],
  },
  'custom-transition': {
    meta: {
      title: 'React のカスタムスクロールアニメーション：イージングと時間',
      description:
        'React のプログラム的スクロールのカスタムイージングと時間：transitionBehavior が目標位置を渡し、あなたが scrollLeft をアニメーション。ライブデモとソース付き。',
    },
    title: 'カスタムスクロールアニメーション：独自のイージングと時間',
    lede: 'ネイティブのスムーズスクロールは、ブラウザが選ぶ 1 つの速度と 1 つの曲線しか与えません。プログラム的スクロールをモーションデザインの残りに合わせたいとき、`noPolyfill={false}` があなたに制御を渡します——メニューがレールの行き先を計算し、あなたのコードが `scrollLeft` をそこまで駆動します。',
    demoHint:
      '矢印をクリックして時間を切り替え——2500 ms では ease-in-out-cubic 曲線が見やすいです。アニメーション途中のクリックは前のものをキャンセルします。',
    prose: [
      {
        heading: '仕組み',
        body: 'デフォルトではメニューはネイティブの `scrollIntoView` でスクロールし、両方のトランジションプロパティを無視します。`noPolyfill={false}` にすると、プログラム的スクロールは scroll-into-view-if-needed ポリフィルを通ります。ポリフィルは目標を計算し、指示としてあなたの `transitionBehavior` に渡します——動くべきスクロール可能祖先ごとに 1 つの `{ el, top, left }` アクションです。ここではメニューが境界として渡すため、常にスクロールコンテナだけです。そこから `animateScroll` は毎 `requestAnimationFrame` で `el.scrollLeft` を目標へ進め、選択した時間にわたって `easeInOutCubic` で進捗をマップします。',
      },
      {
        heading: '進行中のアニメーションの中断',
        body: '2 回目の矢印クリックはアニメーション途中に着地し得ます。ストーリーは要素ごとの保留中フレームを `WeakMap` に保つため、新しい呼び出しは古い `requestAnimationFrame` ループをキャンセルし、2 つが `scrollLeft` を奪い合うことはありません。また各アニメーションは開始点を要素の現在の `scrollLeft` から読むため、新しいものは中断されたものが止まった場所から正確に引き継ぎます。',
      },
      {
        heading: '注意点',
        body: [
          '- ここにはイージング関数と結びついたものはありません——目標位置さえあれば、どの曲線やアニメーションライブラリでも動きます。',
          '- 型は `transitionBehavior` を `ScrollBehavior` 文字列と記述しますが、値はそのまま scroll-into-view-if-needed へ `behavior` コールバックとして渡ります——ゆえにソースにキャストがあります。',
          '- ストーリーは同じ時間 state を `transitionDuration` とアニメーション自体の両方に配線するので、両者がずれません。',
        ].join('\n'),
      },
    ],
  },

  'prevent-body-scroll': {
    meta: {
      title: 'ホイールでのページスクロール防止：React 横メニュー',
      description:
        'ページを動かさずにマウスホイールで React 横メニューをスクロール：ホバーで切り替わるネイティブの非パッシブホイールリスナー。ライブデモと完全なソース付き。',
    },
    title: 'ホイールでメニューをスクロール——ページはスクロールせずに',
    lede: 'マウスホイールの下の横メニューは厄介です。ホイールはページをスクロールし、行は動きません。修正は 2 つに分かれます——ホイールの刻みをページングに変える `onWheel` ハンドラーと、下のページの移動を防ぐネイティブの非パッシブリスナー。後半は React 単独ではできません。',
    demoHint:
      'ポインターを行の上に置いてホイールを回す——行がページングし、ページは動かない。行から離れると、ホイールは再びページをスクロールします。',
    prose: [
      {
        heading: 'ホイールをページングに変える',
        body: '`ScrollMenu` の `onWheel` プロパティは API オブジェクトとホイールイベントとともに呼ばれます。本物のマウスホイールは粗い刻みで Y のみの差分を報告するため、ハンドラーは `deltaY` が負なら `scrollNext`、そうでなければ `scrollPrev` を呼びます——各刻みが行をページングします。その前に、イベントがタッチパッドジェスチャーに見えるか確認します。少しでも `deltaX` がある、または `deltaY` が 15 未満の場合です。',
      },
      {
        heading: 'ページロックにネイティブリスナーが要る理由',
        body: "React ハンドラー内で `preventDefault` を呼ぶのがページを止める明白な方法に思えます——そしてそれは静かに何もしません。React はホイールリスナーをパッシブで登録し、パッシブリスナーはイベントのキャンセルを禁じられているからです。そこで `usePreventBodyScroll` は React を迂回します。`mouseenter` で `document.addEventListener('wheel', preventDefault, { passive: false })` を実行し、`mouseleave` で再びリスナーを除去します。ポインターがメニュー上にある間、すべてのホイールイベントは `document` までバブルし、そこでデフォルト動作——ページのスクロール——がキャンセルされます。`useEffect` のクリーンアップはアンマウント時に `enableScroll` を呼ぶため、ページがロックされたまま残ることは決してありません。",
      },
      {
        heading: 'タッチパッドの逃げ道',
        body: '二本指パンもホイールイベントとして届き、コンテナはそれらからネイティブにスクロールします——document リスナーはそれを殺してしまいます。タッチパッドのヒューリスティックに一致するイベントでは、ハンドラーが `stopPropagation` を呼んでリターンします。イベントは document リスナーに到達せず、ネイティブパンは生き残ります。タッチパッドを検出する信頼できる方法はありません。差分のヒューリスティックはストーリーの正直な推測であり、実務では持ちこたえます。',
      },
      {
        heading: '注意点',
        body: [
          '- ブラウザが document レベルのホイールリスナーをデフォルトでパッシブにしたのは、まさにページのスクロールをジャンクらせないためです——`passive: false` は `preventDefault` を再び合法にする明示的なオプトアウトです。',
          '- ホイール上は前へ、ホイール下は後ろへページングします——それがストーリーのマッピングです。逆にするには `scrollNext` / `scrollPrev` の分岐を入れ替えます。',
          '- タッチデバイスはこれのどれも実行しません。`mouseenter` はなく、行のスワイプは最初からネイティブスクロールです。',
          '- ロックは `mouseenter` と `mouseleave` の間だけ存在するので、ポインターがレールを離れた瞬間、ページの残りは普通にスクロールします。',
        ].join('\n'),
      },
    ],
  },

  'one-item-scroll': {
    meta: {
      title: 'React で一度に 1 項目スクロール：精密なカルーセル矢印',
      description:
        '矢印クリックごとに React カルーセルを 1 項目進める：scrollToItem と getNextElement がページではなく 1 カードずつ進める。ライブデモと完全なソース付き。',
    },
    title: 'ページではなく一度に 1 項目スクロール',
    lede: 'デフォルトでは矢印はページングします。可視のすべてが滑り出て、次のグループが滑り込む。この例はそれらをステップ——1 クリック 1 カード——に配線し直し、変更全体は矢印の `onClick` が呼ぶものだけです。同じメニュー、同じ項目、異なるスクロール先。',
    demoHint:
      '矢印をクリック——行はページではなく 1 カード進みます。矢印は端で無効になります。',
    prose: [
      {
        heading: '仕組み',
        body: '`getNextElement()` は可視グループを越えた最初の項目を返し、`getPrevElement()` はその直前のものを返します。右矢印は `scrollToItem(visibility.getNextElement(), ’smooth’, ’end’)` を呼びます——その項目をコンテナの終端に揃えると、ビューに収めるのにちょうど必要なだけスクロールし、行はちょうど 1 カード動きます。左矢印はその鏡像です。前の要素を `’start’` に揃えます。',
      },
      {
        heading: '整列こそがトリックのすべて',
        body: '標準の `scrollNext()` は同じ次の要素を内部で解決しますが、始端に揃えます——ビューは可視グループ全体を通り越し、その項目を先頭に置きます。1 つの `ScrollLogicalPosition` 引数がページングとステップの違いです。`scrollToItem` の第 3 引数は標準の scroll-into-view の `inline` 整列、第 2 引数は動作で、ここでは `’smooth’` です。',
      },
      {
        heading: '注意点',
        body: [
          '- 矢印状態は `’first’` と `’last’` の短縮形を使います。`useIsVisible(’first’, true)` が先頭で左矢印を、`useIsVisible(’last’, false)` が末尾で右矢印を無効にします。',
          '- 両端で `getNextElement()` は undefined を返し、`scrollToItem` は静かに何もしないので、有効な矢印でもスクロールし過ぎることはありません。',
          '- ストーリーの `onWheel` ハンドラーはホイール 1 ノッチごとに丸々 1 画面をページングします——ステップは矢印の動作であって、グローバルモードではありません。',
          '- 項目のクリックは手つかずです。カードは自身の `onClick` で選択をトグルし、矢印のスクロール方法とは独立しています。',
        ].join('\n'),
      },
    ],
  },

  'items-animation': {
    meta: {
      title: 'React でリスト項目の追加・削除をアニメーション',
      description:
        'React 横リストで項目を追加・削除・シャッフルし、ScrollMenu の containerRef プロパティ経由で @formkit/auto-animate がアニメーション。ライブデモと完全なソース付き。',
    },
    title: 'auto-animate で項目を出し入れし、所定の位置へ',
    lede: '横リストへの追加は新しい項目をポンと現し、削除は隣同士をパチンと寄せます。`@formkit/auto-animate` は単一の親 ref で両方を修正します——そして `ScrollMenu` の `containerRef` プロパティが、それが必要とするまさにその要素を渡します。',
    demoHint:
      '追加、削除、シャッフル——入場、退場、並び替えのすべてがアニメーションされます。メニュー自身にはアニメーションコードがありません。',
    prose: [
      {
        heading: '仕組み',
        body: '`useAutoAnimate()` は、アニメーションすべき要素の直接の親に置かれるべき ref を返します。`ScrollMenu` 内でその親はスクロールコンテナです。渡した各子は item div に包まれ、それら item div がコンテナの直接の子です。ストーリーは ref をそのまま通します——`<ScrollMenu containerRef={parent}>`——auto-animate がそこから引き継ぎます。追加された項目はイーズインし、削除された項目はアニメーションで抜け、並び替えられた項目は新しいスロットへ滑ります。メニュー自身はアニメーションされていることを決して知りません。',
      },
      {
        heading: '追加、削除、シャッフル',
        body: '3 つの操作はすべて items 配列への普通の `setState` 呼び出しです——`addItems` は 1 つ追加し、`removeItems` は最後を落とし、`shuffle` はコピーへの Fisher–Yates パスです。アニメーションは完全に、それらの更新が引き起こす DOM 変異から来ます。1 つのルールを保つ価値があります。`itemId` は React key とメニューの追跡マップ内の項目のハンドルを兼ねるので、id は一意を保つ必要があります——ストーリーは重複を作るリスクを負うより、削除が残した番号の隙間を埋め直します。',
      },
      {
        heading: 'スクロールと追跡は機能し続ける',
        body: 'メニューは子が変わるたびに再観測するので、新規追加項目の `useIsVisible` はすぐに正しく報告し、矢印はページングを続けます。ただし新しい項目はたいてい画面外に着地します——入場を実際に見せたいなら、add-item-and-scroll-to-it 例のように `scrollToItem` と組み合わせます。',
      },
      {
        heading: '注意点',
        body: [
          '- `containerRef` は ref オブジェクトかコールバック ref を受け取ります——`useAutoAnimate` のコールバックは直接差し込めます。',
          '- auto-animate はゼロ設定でフレームワーク非依存。React バインディングは `useAutoAnimate` フック 1 つです。',
          '- 上のデモは id 管理を単調カウンターに簡略化しています。コードパネルはストーリーの隙間埋め版を示します。',
        ].join('\n'),
      },
    ],
  },

  'mui-scrollable-tabs': {
    meta: {
      title: 'MUI のスクロールタブの代替：ネイティブスクロールタブ',
      description:
        'MUI の variant="scrollable" に物足りなくなってきましたか？value/onChange 契約は保ったまま、モバイルでも消えないスクロールボタンを手に入れ、中央寄せとスクロールを両立。完全なソース付き。',
    },
    title: 'MUI を超えるスクロール可能なタブ',
    lede: 'Material UI のスクロール可能なタブは Tabs のセマンティクスに溶接されており、モバイルではデフォルトでスクロールボタンが消えます。このレシピはあなたのコードが依存している部分——`value`/`onChange` 契約——を保ったまま、内部の帯を入れ替えます：ネイティブスクロール、自ら中央に寄る選択、どんな中身でも持てるタブ。',
    demoHint:
      'どちらかの端近くのタブをクリック——自分で中央に寄ります。行はドラッグでも動かせます——携帯と同じように。',
    prose: [
      {
        heading: 'value/onChange 契約を保つ',
        body: 'ソース内の `handleChange` は MUI と全く同じシグネチャ——`(event, newValue)`——を持ちます。移行はマークアップの差し替えを意味し、state の配線し直しではありません：あなたの `useState`、ハンドラー、タブパネルはそのままです。選択は `api.scrollToItem(el, ’smooth’, ’center’)` で自ら中央に寄ります——[center-on-click](/examples/center-on-click) の例とまったく同じ配線です。',
      },
      {
        heading: 'モバイルでも消えないスクロールボタン',
        body: 'MUI は `allowScrollButtonsMobile` で明示的にオプトインしない限り、600px 未満でスクロールボタンを非表示にします——それでも、あくまで Tabs の内部要素です。ここでは矢印はあなた自身のコンポーネントです：`useIsVisible(’first’)` / `useIsVisible(’last’)` が不透明度のフェードを駆動し、あらゆるビューポートで描画され、矢印が何をしていようとタッチスクロールはネイティブのままです。',
      },
      {
        heading: '中央寄せとスクロールを両立',
        body: 'MUI では `centered` プロパティと `scrollable` バリアントは互いに排他的です——ドキュメントはどちらか一方を選ぶよう指示します。ここでは中央寄せはレイアウトモードではなく、クリックごとのスクロールなので、帯は両方を同時に満たします：ネイティブにオーバーフローしつつ、選択された各タブが中央へ滑ります。',
      },
      {
        heading: 'タブでなくなるタブ',
        body: 'デモの 2 つのタブは件数バッジを持ちます。チップやアバター、混在コンテンツも同じように動きます——唯一の要件は `itemId` を持つことです。ソースと同じように `@emotion/styled` でスタイリングしても、Material アプリに馴染ませるために MUI 自身の `styled()` を使っても、あるいは Tailwind を使ってもかまいません。上のデモは [ドラッグスクロール](/examples/mouse-drag) を追加しており、マウント時に選択済みタブを復元するのは [位置の保存と復元](/examples/save-restore-position) です。',
      },
      {
        heading: 'Notes',
        body: [
          '- ARIA パターンを選んでください：実際のパネルが切り替わる場合（ここのように）は `role="tablist"`/`role="tab"`/`aria-selected` を保ち、「タブ」がナビゲーションリンクである場合は `aria-current` を使います。',
          '- ドラッグを有効にする場合は、ドラッグ解放時に発火するクリックを抑制してください——デモは選択前に `dragManager.dragging` を確認しますが、これは [ドラッグのレシピ](/examples/mouse-drag) と同じです。',
          '- [RTL](/examples/rtl) は追加の作業を必要としません：帯はネイティブのスクロールコンテナなので、`direction: rtl` が矢印ごと反転させます。',
        ].join('\n'),
      },
    ],
  },
};

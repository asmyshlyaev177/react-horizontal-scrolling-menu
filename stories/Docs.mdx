import { Meta } from '@storybook/addon-docs/blocks';
import Markdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import README from '../README.md?raw'

<Meta title="Docs" />

export const fallbackDocs = `
**react-horizontal-scrolling-menu** is a horizontal scrolling menu component for React.
It renders your items in a scroll container, tracks their visibility with an
IntersectionObserver, and gives you a full API for arrows, drag, swipe and
programmatic scrolling. See the
[README on GitHub](https://github.com/asmyshlyaev177/react-horizontal-scrolling-menu)
for the full documentation.
`;

export const docsContent = (() => {
  const match = README.match(
    /<!--\s?DOCS_START\s?-->(.*)<!--\s?DOCS_END\s?-->/s,
  );
  return match ? match[1] : fallbackDocs;
})();

<div className="container">
  <div className='section-title'>
    # Docs
  </div>
</div>

## Quick start

Add library to your project

```
npm install --save react-horizontal-scrolling-menu
```

Start by <a href="/?path=/story/examples-simple--simple">simple example</a> and use this documentation as a reference

You can ask for help or propose improvements at <a href="https://github.com/asmyshlyaev177/react-horizontal-scrolling-menu" target="_blank">library's github</a>


<Markdown remarkPlugins={[remarkGfm]}>
{docsContent}
</Markdown>

<style>
  {`
  .container {
    margin-bottom: 48px;
  }

  .section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
  }

  `}
</style>

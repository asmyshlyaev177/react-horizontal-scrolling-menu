/**
 * Formatting is driven by `.editorconfig`, which the Prettier CLI honours by
 * default (pass `--no-editorconfig` to opt out). The `editorConfig` key that
 * used to live here is not a valid config-file option and Prettier 3 warns
 * about it, so it has been removed.
 *
 * @type {import("prettier").Config}
 */
const config = {};

export default config;

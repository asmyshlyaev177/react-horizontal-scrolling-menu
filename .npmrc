# registry.npmjs.ORG is the registry; npmjs.com is the website. The .com host
# answers reads, so installs worked, but publishing there fails: it is not where
# trusted publishing exchanges its OIDC token, and this project-level setting
# overrides the registry actions/setup-node configures.
registry = "https://registry.npmjs.org/"
legacy-peer-deps = true

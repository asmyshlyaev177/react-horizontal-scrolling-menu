name: Test and deploy
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-24.04
    container: cypress/included:cypress-15.19.0-node-24.18.1-chrome-150.0.7871.186-1-ff-153.0.1-edge-150.0.4078.105-1

    env:
      CYPRESS_CACHE_FOLDER: ../cypress_cache
      NODE_OPTIONS: --max-old-space-size=4096
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      CI: true
      WIREIT_LOGGER: 'simple'
    steps:
      - uses: actions/checkout@v7
      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          check-latest: true
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      # v2 of this action is required: GitHub retired the v1 Actions cache
      # service, which made wireit fail with "GitHub Cache check HTTP 400 error".
      # wireit >= 0.14.12 speaks the v2 cache backend.
      - uses: google/wireit@setup-github-actions-caching/v2

      - name: Install
        run: |
          mkdir -p ../cypress_cache && \
          npm run setup && \
          chmod -R 777 ../cypress_cache && \
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | tee /etc/apt/trusted.gpg.d/google.asc >/dev/null && \
          apt update && \
          npx cross-env HOME=/root npx playwright install chromium firefox webkit --with-deps && \
          chmod -R 777 /root/.cache/ms-playwright && \
          apt install -y curl

      - name: Types and linter check
        run: npm run test:lint

      - name: Unit tests
        run: npm run test:unit -- --coverage --coverageReporters="lcov"

      - name: Run codacy-coverage-reporter
        uses: codacy/codacy-coverage-reporter-action@v1.3.0
        continue-on-error: true
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          coverage-reports: coverage/lcov.info

      - name: E2E tests builded version
        run: npm run test:e2e
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: cypress
          path: cypress

      - name: E2E tests dev version
        run: npm run test:e2e:dev

      - name: Storybook tests
        run: npx cross-env HOME=/root npm run test:storybook-ci

  deploy-storybook:
    needs: build-test
    runs-on: ubuntu-24.04
    if: github.ref_name == 'master'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'
      - name: Install
        run: npm run setup
      - name: Build storybook
        run: npm run build-storybook
      - name: Deploy storybook
        uses: peaceiris/actions-gh-pages@v4
        if: github.ref_name == 'master'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./storybook-static

  # publish:
  #   needs: build-test
  #   runs-on: ubuntu-24.04
  #   if: github.ref_name == 'master'
  #   env:
  #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  #   steps:
  #     - uses: actions/checkout@v7
  #     - uses: actions/setup-node@v7
  #       with:
  #         node-version-file: '.nvmrc'
  #         cache: 'npm'
  #         registry-url: 'https://registry.npmjs.org'
  #     - name: Install
  #       run: npm run setup
  #     - run: npm run build
  #     - uses: JS-DevTools/npm-publish@v3
  #       with:
  #         token: ${{ secrets.NPM_TOKEN }}
  #         provenance: true
  #         registry: 'https://registry.npmjs.org'
